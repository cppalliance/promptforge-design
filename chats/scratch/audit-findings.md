# Audit findings - design-principles.md

- Check 2 (No provenance) FAILED: the "Cross-Check Results" section (lines 297-309) is provenance content, naming "the chat record" and "the design documents" and sorting principles into attestation buckets, with incidental source references also at line 13 ("distilled from the design record") and line 332 ("in these chats"). Fix: remove the Cross-Check Results section from the rulebook and drop the two incidental record references.
- Check 6 (Structure) FAILED: "## The Approach Behind the Rules" (line 330) is the only section not wrapped in a uniquely named tag. Fix: wrap it in a single <approach> tag like the other sections.
- Check 7 (Rationale honesty) FAILED: principle 70 (line 207) rationalizes the section model lock with "silently breaks KV-cache locality and hides a provider change from the author", a motive nowhere in the record - the packet's only KV-cache motive concerns flipping a conversation between physical endpoints at the gateway (principle 88), and the model-lock rule is recorded with no such rationale. Fix: re-ground the rationale in the attested record (one explicit, source-visible model choice per section) or delete the KV-cache claim.
