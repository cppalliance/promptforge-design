# Fine-tuning small models

---

> Question, I wanna take a block of, of instructions. I wanna take a prompt. And I want to very quickly compute a little summary, like for a progress indicator, just a few words. For example, the text "searching on the web". And then I wanna but I want this to be fast. Is what is the practicality of fine-tuning a very, very small model? Here's what we do: we, we take the prompt, we remove anything that's not a letter And we convert everything to lowercase, and then we use our own tokenizer that tokenizes entire word and the fragments. And we the tokenizer discards certain words, like it just gets rid of them, it maps them to nothing, so we compress. And then we have a fine-tuned model or even a classifier. And I don't know, is there any existing is it, is there a p are people already doing this? 'Cause like, when I put a prompt in Cursor, like it instantly gives a title to the tab.

This is a well-trodden problem. Let me break down what's practical.

## What Cursor (and everyone else) actually does

The tab-titling trick is almost certainly just a fast, cheap LLM call - something like Claude Haiku or GPT-4o-mini with a system prompt like "title this conversation in 5 words." These models respond in 200-400ms with a short output, and the title can render async (show a spinner or placeholder, swap in the title when it arrives). No fine-tuning, no custom tokenizer, no training data pipeline. It just works.

That's the dominant approach across the industry for this exact task. **Confidence: high** - it's the simplest thing that works and the economics are negligible (pennies per thousand titles).

## Your proposed approach - custom tokenizer + tiny model

What you're describing - strip non-alpha, lowercase, drop stop words, custom tokenizer, then classify - is essentially reinventing extractive summarization with extra steps. The problem is:

1. **You lose semantic understanding.** "Tell me how to fix a segfault in my allocator" and "My allocator segfaults, help" need to produce the same title, but bag-of-words approaches struggle with this.
2. **The training data problem.** You need thousands of (prompt, ideal-title) pairs to fine-tune even a tiny model. Where do those come from? You'd probably generate them with... a large LLM. At which point you've built a distillation pipeline for a problem a single API call solves.
3. **Maintenance burden.** Stop word lists, custom tokenizers, and small classifiers are brittle. New domains break them.

That said, if you truly need **sub-10ms local inference with zero network calls**, here are the realistic options in that direction:

| Approach | Latency | Quality | Effort |
|---|---|---|---|
| TF-IDF / TextRank keyword extraction | <1ms | Low - extracts words, doesn't compose | Trivial |
| Fine-tuned T5-small (60M params) | 10-50ms CPU | Medium-good | Moderate - need training pairs |
| Fine-tuned FLAN-T5-small | 10-50ms CPU | Better - pretrained for instruction following | Moderate |
| Distilled student model from GPT-4o-mini | 10-50ms CPU | Good | Significant pipeline |
| Haiku / GPT-4o-mini API call | 200-500ms | Excellent | Near zero |

## What I'd actually recommend

**Just call a fast small model async.** Show a placeholder title, fire the API call, swap in the result. The user never notices 300ms of latency on a tab title. The cost is effectively zero. The quality is excellent. The engineering effort is one afternoon.

If you're building something where network calls are genuinely unacceptable (offline, embedded, air-gapped), then fine-tune **FLAN-T5-small** on synthetic training data generated by a large model. Skip the custom tokenizer entirely - T5's tokenizer already handles this well. You'd:

1. Generate 5-10k (prompt, title) pairs using Claude or GPT-4o
2. Fine-tune T5-small on them (a few hours on a single GPU)
3. Export to ONNX for fast CPU inference
4. Get ~20ms local inference with decent quality

The custom tokenizer with stop-word removal you described is solving a problem that pretrained tokenizers already solve. The compression you'd gain isn't worth the semantic information you'd lose. **Confidence: high** - this is well-established in the NLP distillation literature.

---

> What if I want to title like a tool call? And what, so, and what if I have in the tool, I give it like some text, and then I train the small model on that text so that the model knows, like, it's, like it's given the tool call as the hint, it's given the name of the tool as the hint?

Good refinement. You're describing something more structured now - not free-form summarization, but essentially:

**Input:** a tool call (tool name + arguments/body text)
**Output:** a human-readable progress label like "Searching the web" or "Reading file config.toml"

This is a much easier problem than general summarization, and the tool name is a huge hint. Let me think through this clearly.

## Why this is actually a classification/template problem, not summarization

You don't have infinite possible outputs. You have a finite set of tools, and each tool produces a narrow range of sensible labels:

- `WebSearch(query="rust async traits")` → "Searching the web"
- `Read(path="/src/main.rs")` → "Reading main.rs"
- `Shell(command="npm test")` → "Running tests"
- `Grep(pattern="TODO")` → "Searching for TODOs"

The tool name tells you the *verb*. The arguments tell you the *object*. The label is just `verb + object`. You might not even need a model at all.

## Three approaches, simplest first

### 1. Template mapping (no model, microseconds)

```python
TEMPLATES = {
    "WebSearch": lambda args: f"Searching the web for '{args.get('query', '')[:30]}'",
    "Read":      lambda args: f"Reading {Path(args['path']).name}",
    "Shell":     lambda args: f"Running {args['command'].split()[0]}",
    "Grep":      lambda args: f"Searching for '{args['pattern'][:20]}'",
    "Write":     lambda args: f"Writing {Path(args['path']).name}",
}

def label(tool_name, args):
    return TEMPLATES.get(tool_name, lambda a: f"Using {tool_name}")(args)
```

Zero latency, zero cost, perfectly predictable. Covers 80% of cases. This is probably what most production systems actually do. **Confidence: high.**

### 2. Small model with tool name as a conditioning hint

This is your idea. You'd train a model where the input is structured like:

```
[TOOL:WebSearch] query="how to fix segfault in custom allocator"
```

And the target output is:

```
Searching for segfault fixes
```

The `[TOOL:WebSearch]` prefix acts as a learned routing signal - the model learns that `WebSearch` maps to "Searching..." patterns, `Read` maps to "Reading..." patterns, etc. This works well because:

- The tool name massively constrains the output distribution
- The model only needs to learn how to extract and compress the salient noun phrase from the arguments
- A tiny model (T5-small, 60M params) could handle this easily
- You could train it on maybe 1-2k synthetic examples per tool

The training data generation is trivial - you have real tool calls from your logs, and you can batch-label them with a big model in one pass. The fine-tuned model would run in ~10-20ms on CPU.

### 3. The hybrid - templates with a model fallback

Use templates for known tools. Fall back to a small model (or a fast API call) for unknown/custom tools or when the arguments are complex enough that a template produces garbage.

## My honest assessment

For tool call labeling specifically, **templates win**. The structure is too regular and predictable to justify training infrastructure. You have a closed set of tools. Each tool has a known schema. The "creative" part - turning `query="rust async trait bounds lifetime"` into something like "Searching for Rust trait bounds" - is nice-to-have but not necessary. "Searching the web" is perfectly fine as a progress indicator.

The small-model approach becomes worth it when:
- You have dozens or hundreds of tools and don't want to write templates for all of them
- The arguments are complex enough that extracting the salient phrase matters
- You want the labels to feel natural/varied rather than mechanical

If you do go the model route, skip the custom tokenizer. Use T5-small with the tool name prepended to the input. The pretrained tokenizer already handles this well, and the tool name prefix gives you the routing signal you described. **Confidence: high** - this is standard conditional text generation, well-supported by the T5 architecture.

---

> The thing though, like, I'm, I wanna build PromptForge into a system that does big results on small hardware, like 48 gigabytes of video card. So I don't wanna call into Haiku, I want something small and fast, and I don't want any latency. I want, I'm talking like a hundred million parameters with the very clever tokenizer. In fact, what about a combination? Why don't we do How about if we do a clever tokenizer? And we have a multi-uh and we have a classifier that, like, a multi-signal classifier that generates twenty-five signals, and then those signals map to a template, or they map to like some text, a and/or they map to some kind of token, and then we train, we fine-tune a model, a very small model. So we fine-tune a hundred million parameter model on the specialized tokens and a signal from a twenty-five shot Classifier, and we use a frontier model to generate the Summary. And I want it fast, like I want like Fifty milliseconds.

OK, now you're cooking. Let me engage with this architecture seriously because it's actually defensible. You're describing a two-stage pipeline:

## The Architecture

```
Raw prompt
  ↓
Custom tokenizer (strip, lowercase, stop-word removal, domain BPE)
  ↓  compressed token sequence
25-signal classifier (fast, maybe linear on embeddings)
  ↓  [sig_0=tool_search, sig_1=topic_web, sig_2=action_query, ...]
Small generative model (100M params, conditioned on signals + tokens)
  ↓
"Searching the web for Rust traits"
```

## Why this actually works

The 25-signal classifier is the key insight. You're doing **feature extraction before generation**, which means the small model doesn't have to figure out *what's happening* - it already knows. It just has to compose a phrase. That's a dramatically easier task for 100M parameters.

Think of each signal as a learned routing variable:

- **Signal 0-4:** Action class (search, read, write, compute, communicate)
- **Signal 5-9:** Domain/topic cluster
- **Signal 10-14:** Object type (file, URL, code, text, data)
- **Signal 15-19:** Specificity level, complexity, urgency, etc.
- **Signal 20-24:** Key entity flags

The classifier can be a single linear layer on top of a small frozen embedding model. Something like a 30M param embedding encoder (e5-small, or even a distilled sentence transformer) with a learned classification head. That runs in **2-5ms on GPU**.

Then the generative model gets input like:

```
[ACT:search][DOM:web][OBJ:query][ENT:rust_traits] rust async trait bounds
```

With those signals prepended, a 100M param model barely has to think. It's practically a lookup at that point.

## Latency budget (50ms target on 48GB GPU)

| Stage | Model | Time | Notes |
|---|---|---|---|
| Tokenizer | Rule-based + BPE | <1ms | Pure CPU, no model |
| Classifier | Linear head on small embeddings | 3-8ms | 30M params, single forward pass |
| Generator | Fine-tuned T5-small or custom | 10-30ms | 60-100M params, ~10 output tokens |
| Overhead | Tensor moves, postprocess | 2-5ms | |
| **Total** | | **15-44ms** | Comfortably under 50ms |

This fits. Easily. A 100M param model generating 10 tokens on a 48GB GPU is nothing. You'd be using maybe 400MB of VRAM. You could run 100 of these models simultaneously and not fill that card.

## The training pipeline

This is where the frontier model comes in:

**Step 1 - Generate training data:**
Take your real prompt logs. Send each one to Claude/GPT-4o with:
```
Given this prompt/tool call, produce:
1. A JSON object with 25 categorical signals (schema provided)
2. A short human-readable summary (3-8 words)
```

Generate 10-50k labeled examples. Cost: maybe $20-50.

**Step 2 - Train the classifier:**
Fine-tune the embedding model's classification head on the 25 signal labels. This is a standard multi-label classification task. Trains in minutes.

**Step 3 - Train the generator:**
Fine-tune T5-small (or train a custom 100M param decoder) where the input is `[signal tokens] + [compressed prompt tokens]` and the target is the summary. Trains in an hour or two on a single GPU.

**Step 4 - Validate with the frontier model:**
Generate new prompts, run them through your pipeline, have the frontier model grade the outputs. Iterate.

## The custom tokenizer - what's actually worth doing

Your instinct about tokenization is right but the specific approach (strip non-alpha, lowercase) loses too much. Here's what I'd actually do:

- **Keep the standard BPE tokenizer** from your base model (T5, etc.)
- **Add special tokens** for each of your 25 signal classes: `[ACT:search]`, `[DOM:web]`, etc.
- **Prepend the signal tokens** to the input sequence
- **Truncate the prompt** to maybe 64-128 tokens - you don't need the whole thing for a summary

The "compression" comes from truncation + the signals carrying the heavy semantic load. The tokenizer's job is just to pass through enough of the prompt for the model to extract a good noun phrase.

If you really want custom vocabulary compression, train a **domain-specific BPE** on your prompt corpus. This would give you better compression ratios on your specific distribution (code-heavy prompts, tool calls, etc.) than a general-purpose tokenizer. But this means training a model from scratch rather than fine-tuning T5, which is more work.

## My honest take

This architecture is sound. It's a legitimate distillation pipeline with a clever decomposition (classifier handles *understanding*, generator handles *phrasing*). The 50ms budget is generous for this workload on GPU.

The risk is **over-engineering relative to the task**. You're building a two-model pipeline with custom tokenization and a training data generation system to produce 5-word progress labels. If this is a building block for PromptForge's broader capabilities - summarization, routing, intent detection - then the infrastructure pays for itself across multiple uses. If it's just for progress labels, templates are 1000x less work. **Confidence: high.**

The 25-signal classifier is the most reusable piece. Once you have it, it's useful for routing, caching, dedup, monitoring - not just labeling.

---

> I think you're wrong about this, because this is what I'm, this is, I wanna build, like, I wanna build a Rust application, I wanna build a natively compiled executable where you run it and, and then it gives an, an MCP interface and then you plug it into like Claude Code or Cursor and now you can call PromptForge prompts, but I wanna have the layer where it communicates to the IDE like what it's doing, where like it shows, so I want the MCP call to look like a subagent and I want it to be, look like the native UI where you see like a little spinning wheel and like it tells you what it's doing, and I want it to be really nice. Don't, and so the en the over-engineering, it's not over-engineering, it's marketing, it's making the user feel good, don't undersell it.

You're right, I was wrong to frame it as over-engineering. This is product feel. The difference between "Processing..." and "Analyzing your test configuration for edge cases" is the difference between a script and a product. Point taken.

## The actual product architecture

You're building an MCP server in Rust. When Cursor or Claude Code calls a PromptForge tool, the user should see something that looks and feels native - a spinner with intelligent, contextual status text that updates as the pipeline progresses. MCP has exactly the mechanism for this: `notifications/progress` with a `message` field. That message field is your canvas.

```
User calls PromptForge MCP tool
  ↓
Cursor shows: ⟳ Assembling governance analysis prompt...
  ↓  (200ms later)
Cursor shows: ⟳ Injecting 12 source citations...
  ↓  (500ms later)
Cursor shows: ⟳ Running inference on policy comparison...
  ↓
Cursor shows: ✓ Done - 3 sections, 2,400 tokens
```

That feels like a first-party subagent. That's what makes someone think "this tool is serious." Templates can't do that because the messages need to reflect the *actual content* - the specific prompt being assembled, the specific data being injected.

## Rust implementation path

For local inference in a Rust binary, you have two serious options:

**Option A: ONNX Runtime via `ort`** (recommended)
- Export your fine-tuned model to ONNX
- Use the `ort` crate - mature, well-maintained, GPU support via CUDA/DirectML
- Bundle the ONNX model file with your binary (or download on first run)
- Inference is straightforward: tokenize → run session → decode
- 100M param model in ONNX: ~200-400MB file, loads in <1s, infers in 10-30ms on GPU

**Option B: `candle` (Hugging Face's Rust ML framework)**
- Native Rust, no C++ dependency
- Supports T5, BERT, and custom architectures
- More control, but more code to write
- CUDA support, Metal support on Mac

I'd go with `ort`. It's battle-tested, the ONNX export path from PyTorch is well-documented, and you don't have to reimplement model architectures in Rust.

## The model + classifier in the Rust binary

```
┌─────────────────────────────────────────┐
│  PromptForge MCP Server (Rust binary)   │
│                                         │
│  ┌─────────────┐   ┌────────────────┐   │
│  │  Classifier  │   │   Generator    │   │
│  │  (ONNX, 30M) │──→│  (ONNX, 100M)  │   │
│  └──────┬───────┘   └───────┬────────┘   │
│         │                   │            │
│    25 signals          "Scanning your    │
│                     test configuration"  │
│         │                   │            │
│         └───────┬───────────┘            │
│                 ↓                        │
│     notifications/progress { message }   │
└─────────────────────────────────────────┘
```

Both models load on startup. The classifier and generator share the GPU. Total VRAM: maybe 500MB-1GB. On a 48GB card running actual inference workloads, this is rounding error.

## What the progress stream actually looks like in code

```rust
// During prompt assembly, emit contextual progress
async fn emit_progress(
    ctx: &McpContext,
    token: &ProgressToken,
    stage: PipelineStage,
    prompt_content: &str,
) {
    let signals = self.classifier.classify(prompt_content);
    let message = self.generator.summarize(&signals, prompt_content, stage);
    
    ctx.notify_progress(token, Progress {
        progress: stage.ordinal() as f64,
        total: Some(stage.total() as f64),
        message: Some(message),
    }).await;
}
```

Each pipeline stage fires a progress notification. The classifier + generator produce a contextual message in <50ms. The user sees it update in real time. It feels alive.

## Training the models for this specific use case

The training data is PromptForge-specific. You'd generate examples like:

| Pipeline stage | Prompt content (truncated) | Target message |
|---|---|---|
| `template_load` | `governance-analysis-v3` | "Loading governance analysis template" |
| `source_inject` | `[12 citations from WG21 papers]` | "Injecting 12 committee paper citations" |
| `variable_bind` | `{subject: "P2300", period: "2024-2026"}` | "Binding analysis to P2300 history" |
| `inference_start` | `Compare voting patterns across...` | "Running voting pattern comparison" |
| `postprocess` | `3 sections, 2847 tokens` | "Formatting 3-section report" |

The pipeline stage is a strong signal (like the tool name from before), and the prompt content gives the model enough to produce something specific and intelligent. Generate 10-20k examples with a frontier model, fine-tune, export to ONNX, ship it in the binary.

## The marketing angle you're describing

You're right that this is marketing. When someone plugs PromptForge into Cursor and sees intelligent, real-time progress that matches what's actually happening - not generic spinners, not "please wait" - that's the moment they think "this is built by someone who cares." It's the same reason Apple animates everything. The animation isn't the feature. The feeling is the feature.

The 25-signal classifier + 100M param generator is the right architecture for this. It's fast enough to feel instant, smart enough to feel contextual, and small enough to ship inside a single binary. **Confidence: high.**

---

> But I want the, for the classifier and for the model, I don't want anything that's specific to the prompts. Like you're saying, you know, compare voting patterns or running voting pattern, like I don't want voting pattern to be baked in. I want the verb, like vote, to be, like something like that. I wanted to know, I wanted to understand English, but I want this thing to be generic because it has to work with anybody's prompt. And also. If what if the prompt, like if the prompt is really big, then and we only classify the first part, then we might end up with something like wrong, like we might end up with like a sub-agent discipline string, and that's no good. What we need is, I wanna take, I wanna be able to handle prompts of arbitrary size. So for example, what I wanna do is, let's say I have a big prompt, I divide it up into pieces up to the limit of this, of what the classifier can take, and then I classify each piece. So let's say I have like twenty, I have twenty like prompts, twenty classifications, then I wanna take all that and I wanna put that back into the classifier and I wanna say summarize this. Would that work like a recursive summarization?

Two sharp observations here. Let me address both.

## Generality - no domain vocabulary baked in

You're right. The training data can't be "WG21 governance prompts → WG21 governance labels." It has to be "arbitrary English prompts → generic action/object descriptions." The model needs to learn the *pattern* - extract the verb, extract the object, compose a phrase - not memorize domain mappings.

This means:

**The 25 signals must be domain-agnostic categories:**
- Action class: analyze, search, generate, compare, transform, extract, validate...
- Object class: text, code, data, configuration, document, image...
- Complexity: simple, compound, multi-step
- Tone: informational, imperative, conversational
- Structure: single task, pipeline, conditional

**The training data must be diverse:**
You'd generate training pairs from prompts across every domain - coding, writing, research, data analysis, devops, math, creative. The frontier model labels them with generic summaries. "Comparing two database schemas" not "Comparing PostgreSQL migration files for the auth service." The model learns to extract "comparing" + "schemas" from any prompt about comparing schemas, regardless of domain.

The pretrained T5 backbone already understands English. Fine-tuning just teaches it the *task format* - "given this text, produce a short action phrase." That generalizes. **Confidence: high.**

## Long prompts - the chunking problem

This is the harder problem and your instinct about recursive processing is right. Here's why the naive approach fails:

```
[Chunk 1: System prompt boilerplate]     → "Configuring system parameters"  ← WRONG
[Chunk 2: Context injection]             → "Loading reference data"
[Chunk 3: Actual task buried here]       → "Comparing database schemas"    ← RIGHT
[Chunk 4: Output format instructions]    → "Formatting output as JSON"
```

If you only classify chunk 1, you get garbage. If you classify all chunks independently, you get four competing answers. You need aggregation.

## Three approaches, increasing sophistication

### Approach 1: Signal pooling (simplest, probably sufficient)

Don't recurse through the classifier. Separate the embedding step from the classification step:

```
Chunk 1 → encoder → embedding_1 (vector)
Chunk 2 → encoder → embedding_2
Chunk 3 → encoder → embedding_3
Chunk 4 → encoder → embedding_4
              ↓
    Pool (weighted mean)
              ↓
    Single pooled embedding
              ↓
    Classification head → 25 signals
              ↓
    Generator → "Comparing database schemas"
```

The encoder runs on each chunk (~3ms each), but it's just the embedding - no classification head, no generation. Then you pool all embeddings into one vector and classify once. The classification head and generator each run once.

**Budget for a 50k token prompt (100 chunks):**
- Encoder on 100 chunks: 100 × 3ms = 300ms ← too slow

So you **subsample**. You don't need every chunk. Take:
- First chunk (system prompt/context)
- Last chunk (usually the actual instruction)
- 3-5 evenly spaced chunks from the middle
- Maybe the shortest chunk (often the most information-dense)

That's ~7 chunks. 7 × 3ms = 21ms for encoding. Add classification + generation = 35-45ms total. Under budget.

### Approach 2: Recursive summarization (your proposal)

```
Chunk 1 → classifier → "configuring, system, setup"
Chunk 2 → classifier → "loading, reference, data"  
Chunk 3 → classifier → "comparing, database, schema"
Chunk 4 → classifier → "formatting, output, json"
              ↓
Concatenate mini-summaries → new input
              ↓
Classifier again → 25 final signals
              ↓
Generator → "Comparing database schemas"
```

This works and it's what you described. The risk is latency - you're running the classifier N+1 times. But if you subsample to 5-7 chunks, it's:
- 7 classification passes × 5ms = 35ms
- 1 aggregation classification pass = 5ms
- 1 generation pass = 15ms
- Total: ~55ms

Tight but doable. And the recursive step is powerful because the classifier sees all the mini-summaries in context and can identify which one is the *main action* vs. which are setup/formatting.

### Approach 3: Attention-weighted pooling (most elegant)

Train a small attention layer that learns *which chunks matter most*:

```
Chunk embeddings: [e1, e2, e3, e4]
              ↓
Attention weights: [0.05, 0.10, 0.75, 0.10]
              ↓
Weighted sum → single embedding
              ↓
Classifier + Generator
```

The attention layer learns that "the chunk containing an imperative verb is the important one." This is a tiny addition (~100k params) but it solves the "boilerplate at the beginning" problem elegantly. The model learns to ignore system prompts and focus on the task.

## What I'd actually build

**Approach 1 with a twist**: subsample chunks, but be smart about which ones. Use a trivial heuristic to pick high-signal chunks before running the model:

```rust
fn select_chunks(chunks: &[&str], max: usize) -> Vec<usize> {
    let mut selected = Vec::new();
    
    // Always include first and last
    selected.push(0);
    selected.push(chunks.len() - 1);
    
    // Score middle chunks by imperative verb density
    // (questions, commands, action words)
    let mut scored: Vec<(usize, f32)> = chunks.iter()
        .enumerate()
        .skip(1)
        .take(chunks.len() - 2)
        .map(|(i, c)| (i, action_density(c)))
        .collect();
    
    scored.sort_by(|a, b| b.1.partial_cmp(&a.1).unwrap());
    
    for (idx, _) in scored.iter().take(max - 2) {
        selected.push(*idx);
    }
    
    selected.sort();
    selected
}
```

`action_density` is pure heuristic - count imperative verbs, question marks, "please", "you should", etc. No model needed. It just picks the chunks most likely to contain the actual task. Then those 5-7 chunks go through the real model.

Total pipeline:
1. **Chunk** the prompt (rule-based, <1ms)
2. **Select** high-signal chunks (heuristic, <1ms)
3. **Encode** 5-7 chunks (15-21ms)
4. **Pool** embeddings (weighted mean, <1ms)
5. **Classify** pooled embedding (3-5ms)
6. **Generate** summary (10-15ms)

**Total: 30-43ms.** Comfortably under 50ms. Handles arbitrary prompt sizes. Domain-agnostic. Ships in a Rust binary with two ONNX models. **Confidence: high.**

---

> Spawn multiple sub-agents and search the internet for existing practice, maybe even on Hugging Face, look to see what other people say about this problem and their possible solutions, and then also search specifically about each of our solutions like chunking, high-signal selection, cool embedding, encoding, classification, you know, sniff around. And then compile re-analyze the results, and then compile a report into the chat with your advice.

I'll fan out multiple research subagents in parallel to cover different angles, then synthesize.

*[Subagent: general-purpose - no summary available]*

*[Subagent: general-purpose - no summary available]*

*[Subagent: general-purpose - no summary available]*

*[Subagent: general-purpose - no summary available]*

Now I see the schema. Let me launch all four research subagents in parallel.

*[Subagent: Research: fast prompt titling practice]*

Research complete. Full report written to `cabinet/_research/2026-07-25-conduct-research-fast-prompt-titling.md`.

**Top-line findings:**

- **Products universally** use background API calls to their cheapest/fastest model (gpt-4o-mini, Copilot utility model, dedicated task model) triggered after the first exchange. Title generation never blocks the user.

- **The best open-source engineering documentation** is from oh-my-pi (OMP), which systematically benchmarked sub-1B models for local title generation. Their key technique: **prefill the assistant turn with `<title>` and stop at `</title>`**, greedy decoding, no few-shot (few-shot hurts sub-0.6B models). **flan-t5-small was explicitly rejected** - it just echoes input.

- **Purpose-built title models** now exist at 50M-270M params: SupraLabs/supra-title-50M (custom 50M foundation model), KHROTU/titlegemma (Gemma 270M LoRA, 48K training pairs), and gghfez/openwebui-title-generator-270m (Gemma 270M, 8.9K examples).

- **OMP's leaderboard** for local CPU title gen: LFM2-350M (best speed/quality at 212MB), Qwen3-0.6B (most robust), Gemma 270M (smallest viable), SmolLM2-135M (too small).

- **For progress/status text**, the industry splits between static per-tool messages (Delight.ai's "Thinking Messages") and periodic LLM-fork summarization every ~30s (Claude Analysis pattern). No one is using small local models for dynamic status generation.

- **Distillation papers** show you can get 90%+ accuracy at 40% size reduction via shrink-and-fine-tune, and 568M models competitive with ChatGPT via information-theoretic self-training (InfoSumm).

*[Subagent: Research: chunking & recursive summarization]*

Research complete. I ran 16 web searches and compiled everything into `cabinet/_research/2026-07-25-chunking-recursive-summarization-techniques.md`. Here's the high-level taxonomy of what I found:

**1. Recursive Summarization** - Chunk -> summarize -> concatenate -> repeat until fits. Simplest approach. Fidelity degrades with each recursion level. Requires log(N/W) levels.

**2. Map-Reduce / Refine (LangChain)** - Three chain types: Stuff (single call), Map-Reduce (parallel chunk summaries + combine), Refine (sequential rolling summary). Map-reduce is parallelizable; refine preserves cross-chunk context but is serial.

**3. Hierarchical Summarization (6 major papers):**
- **RAPTOR** (ICLR 2024) - Recursive clustering + summarization tree, +20% on QuALITY
- **DTCRS** (ACL 2025) - Query-dependent dynamic trees
- **Context-Aware Hierarchical Merging** (ACL 2025) - Citation-based context retrieval at each merge level
- **BooookScore** (ICLR 2024) - Incremental updating vs. hierarchical merging for books; found incremental = more detail but lower coherence
- **HERA** (2025) - Context packaging + reordering before summarization
- **Hierarchical MDS** - Hierarchy benefits smaller models (Mistral-7B) more than large ones

**4. Attention-Weighted Chunk Pooling:**
- **FETILDA** - Bi-LSTM + self-attention over chunk encodings for weighted document vectors
- **ChuLo** - Keyphrase-based variable-size chunks with chunk-level attention
- **PARADE** - CNN/transformer aggregation over passage representations outperforms score aggregation
- **Late Chunking** (Jina AI) - Encode full document first, then pool into chunk embeddings (preserves context)

**5. Chunk Selection Heuristics:**
- TextRank + BM25/BM25+ for ~3% ROUGE improvement over baseline
- PACSUM (BERT-based centrality) reduces input 60-70% in legal summarization
- NVIDIA recommends page-level chunking as default
- Adaptive Chunking paper proposes 5 intrinsic quality metrics (ICC, DCC, BI, RC, SC)

**6. Embedding Pooling for Classification:**
- Mean pooling of chunk embeddings trains classifiers that also work on individual chunks (enables document-to-chunk label propagation)
- Max pooling better for needle-in-a-haystack; mean pooling for general classification

**7. Architectural Approaches for Unlimited Input:**
- **Unlimiformer** (NeurIPS 2023) - kNN cross-attention, plug-and-play, unlimited input
- **SLED** - Sliding encoder + fusion-in-decoder, ~16K limit
- **CachED** (TACL 2025) - Gradient caching enables 500K+ token training
- **Infini-attention** (Google 2024) - Compressive memory in attention layer, 1M tokens
- **LLMLingua/LongLLMLingua** (Microsoft) - Up to 20x prompt compression via perplexity-based token removal

*[Subagent: Research: Rust ML inference & classifier architectures]*

Research is complete. The full report is at `cabinet/_research/2026-07-25-rust-ml-inference-classifier-architectures.md`. Here's the executive summary:

---

## Part 1 - Rust ML Inference

**The `ort` crate** (v2.0.0-rc.12, 13M downloads, 466 reverse deps) is the production standard. It wraps ONNX Runtime 1.24 with support for CUDA, TensorRT, OpenVINO, CoreML, DirectML. Performance: 3-5x faster than Python, 60-80% less memory, 48MB Docker images. Production users include Google Magika, SurrealDB, HuggingFace TEI, and Supabase.

**The `candle` crate** (v0.6) is HuggingFace's pure-Rust alternative - zero C++ deps, native safetensors, WASM support. Under load testing it delivered P95 of 20ms and 994 RPS vs Python PyTorch's 95ms/222 RPS. Limitation: element-wise ops are slower than PyTorch due to missing kernel fusion.

**Shipping models in binaries**: Three approaches - `include_bytes!` + ort, Burn's `LoadStrategy::Embedded` (compiles ONNX to native Rust code), and Mozilla's Encoderfile (single zero-dep executable with built-in REST/gRPC).

**ONNX export** is straightforward: `optimum-cli export onnx --model google-t5/t5-small --optimize O3` produces the encoder + decoder files.

## Part 2 - Classifier Architectures

**Multi-label classification**: Standard is shared encoder -> N binary sigmoid heads. Advanced: TMSFE projects text and labels into shared latent space with conditional similarity scoring.

**Classifier-conditioned generation**: GeDi uses Bayes rule over the full vocabulary (1000x cheaper than PPLM). DGLM uses diffusion proposals guided by simple logistic regression classifiers. Prompt Gating allows zero-shot combination of aspect-specific plugins.

**Two-stage pipelines**: CAG (EMNLP 2025) decomposes input, classifies stages, then does stage-specific few-shot generation. Hierarchical TGSC uses parent classifications to condition child-level decisions.

**Small classifiers**: TinyBERT-4 at 14.5M params gives 96.8% of BERT-base quality with 9.4x speedup (42ms inference, 55MB). INT8 quantized: 23ms with <0.5% accuracy loss.

*[Subagent: Research: Hugging Face small summarization models]*

Research is complete and filed to `cabinet/_research/2026-07-25-small-hf-models-summarization-classification.md`. Here's the executive summary:

## Key Findings

### Summarization
- **Falconsai/text_summarization** (60.5M) - most popular T5-small summarizer, 1.8M all-time downloads, ONNX version available at `onnx-community/text_summarization-ONNX`
- **shorecode/t5-efficient-tiny-summarizer-general-purpose-v3** (15.6M) - smallest practical summarizer found, ROUGE-L 0.41, 7x compression ratio
- **ooor/t5-small-distilled-summarization** (60.5M) - knowledge-distilled from T5-base teacher

### Title/Label Generation
- **agentlans/flan-t5-small-title** (77M) - purpose-built title generator on flan-t5-small, input format `topic || text`
- **Dafilab/chat-title-generator** (77M) - specifically for generating short titles from chat text, input format `short title: text`
- **AventIQ-AI/t5-news-title-creation** (60.5M) - generates 5-20 token news titles

### Headline Generation
- **JulesBelveze/t5-small-headline-generator** (60.5M) - ROUGE-1: 44.24, ROUGE-L: 41.11, best documented scores
- **tarekziade/t5-small-headline-generator-sft-3-3** (38.5M) - 50% layer-pruned from the above, retains 92%+ quality, ~50 MiB quantized

### Topic Classification
- **MoritzLaurer/deberta-v3-xsmall-zeroshot-v1.1-all-33** (22M backbone, MIT) - best quality-per-parameter for zero-shot, designed for edge/browser
- **typeform/distilbert-base-uncased-mnli** (67M) - classic zero-shot classifier via NLI
- **Mozilla/content-multilabel-iab-classifier** (33.4M) - multi-label IAB content categories on MiniLM backbone

### ONNX Tooling
- **Pre-exported:** `optimum/t5-small` (15K downloads/month), `onnx-community/text_summarization-ONNX`
- **Export CLI:** `optimum-cli export onnx --model MODEL output/`
- **fastT5 library:** One-line ONNX export + INT8 quantization for any T5 model; 3x size reduction, up to 5x speedup

All four research agents are back. Let me pull the full reports and synthesize.

All four research reports are in. Here's the synthesis.

---

## Research Synthesis: Fast Local Progress Text Generation for PromptForge

### The big surprise: nobody is doing what you want to do

The research reveals a clear gap. The industry splits into two camps for progress/status text:

1. **Static per-tool messages** - "Searching the web...", "Reading file..." hardcoded per tool (Delight.ai, most MCP servers)
2. **Periodic LLM fork** - fork the conversation to a frontier model every ~30 seconds and ask "what are you doing?" (Claude Analysis pattern)

**Nobody is running a small local model to generate dynamic, contextual progress text.** This is genuinely novel. If you ship this and it feels good, it's a real differentiator.

### Critical finding: FLAN-T5-small is dead for this task

The [OMP (oh-my-pi) project](ed1c939e-e57b-4bfe-af41-e387d6922c1f) did systematic benchmarking of sub-1B models for local title generation and **explicitly rejected flan-t5-small** - it just echoes the input. This kills my earlier suggestion of fine-tuning T5-small. The T5 encoder-decoder architecture at 60-80M params is fine for *summarization* (compress a paragraph) but fails at the *titling/labeling* task (compose a novel short phrase from long input).

**What actually works for tiny title generation:**

| Model | Params | Disk | Quality | Notes |
|---|---|---|---|---|
| LFM2-350M (Liquid AI) | 350M | 212MB Q4 | Best speed/quality | OMP's #1 pick, ONNX available |
| Gemma 270M (fine-tuned) | 268M | 551MB | Good | Smallest viable per OMP, multiple fine-tunes exist |
| SupraLabs Supra-Title-50M | 50M | ~50MB | Purpose-built | Custom foundation model, trained on 150K title pairs |
| SmolLM2-135M | 135M | ~80MB | Marginal | OMP says "too small" |

### Revised architecture recommendation

Based on the research, here's what I'd build differently from what we discussed:

**Drop the T5-based generator. Use a small decoder-only model instead.**

The evidence is clear: decoder-only models (Gemma 270M, LFM2-350M) outperform encoder-decoder models (T5-small, FLAN-T5) at this task. The "prefill trick" - starting the assistant turn with `<title>` and using greedy decode - gives you tight output control without few-shot examples, which actually *hurt* sub-600M models.

**Keep the classifier. It's your edge.**

The 25-signal classifier is where your architecture genuinely improves over what exists. Here's why: every existing title generation model takes raw text and produces a title. Your architecture extracts structured signals *first*, which means:

- The generator gets a much easier job (signal-conditioned generation, not raw comprehension)
- You can use a *smaller* generator because the classifier carries the semantic load
- The signals are reusable for routing, caching, monitoring

For the classifier, the research points to **TinyBERT-4** (14.5M params, 42ms on CPU, 96.8% of BERT quality) or **DeBERTa-v3-xsmall** (22M active params, MIT license, designed for edge). Both export cleanly to ONNX.

### The refined pipeline

```
Raw prompt (arbitrary length)
  ↓
Chunk + subsample (heuristic, <1ms)
  ↓  5-7 high-signal chunks
TinyBERT-4 encoder (14.5M, ONNX)
  ↓  chunk embeddings
Attention-weighted pooling (learned, ~100K params)
  ↓  single document embedding
25-signal classification head (linear, ~50K params)
  ↓  [ACT:analyze, DOM:code, OBJ:configuration, ...]
Signal-conditioned small decoder (Gemma 270M or SupraTitle-50M, ONNX Q4)
  ↓  prefill with <status>, greedy decode, stop at </status>
"Analyzing your test configuration"
```

**Estimated latency on 48GB GPU:**

| Stage | Time |
|---|---|
| Chunk + subsample | <1ms |
| TinyBERT-4 encode 7 chunks | 5-10ms |
| Attention pool + classify | <1ms |
| Signal-conditioned decode (Gemma 270M Q4) | 15-25ms |
| Overhead | 2-3ms |
| **Total** | **23-40ms** |

That's comfortably under 50ms. On a 48GB GPU, these models are trivial - maybe 300MB VRAM total.

### The chunking question - what the research says

Your recursive summarization idea is validated by the literature (RAPTOR, LangChain map-reduce, hierarchical merging), but for *your specific task* (generating a 5-word label, not a paragraph summary), the research points to a simpler approach:

**Extract-then-abstract beats recursive summarization for short outputs.**

The key insight from the [chunking research](9153f933-ee53-42c2-a554-84dd68286e43): when your target output is very short (5-8 words), you don't need to recursively compress the full document. You need to *find the important parts* and generate from those. This is exactly your subsample + attention-pooling approach.

The **FETILDA** paper validates attention-weighted chunk pooling - a BiLSTM + self-attention over chunk encodings learns which chunks matter. The **PARADE** paper confirms that aggregation over passage representations (your approach) outperforms simple score aggregation (like max-of-chunk-scores).

For chunk selection heuristics, the research suggests:
- **TextRank + BM25** for ~3% improvement over random selection
- **PACSUM** (BERT-based centrality) can reduce input by 60-70% while retaining salient content
- But for your case, a simple heuristic (first chunk, last chunk, highest-action-density middle chunks) is probably sufficient since the attention layer will do the real weighting

### Rust implementation - the stack is ready

The [Rust ML research](9556918c-142b-4b97-9a00-78414d042da2) confirms the stack is production-grade:

**Use `ort`** (not candle) for this project:
- 13M downloads, 466 reverse dependencies
- 3-5x faster than Python PyTorch, 60-80% less memory
- CUDA, TensorRT, DirectML support
- Production users: HuggingFace TEI, Google Magika, SurrealDB
- `include_bytes!` for embedding models in the binary

**ONNX export is straightforward:**
```bash
optimum-cli export onnx --model google/t5-efficient-tiny --optimize O3 output/
```

**Shipping models in the binary**: use `include_bytes!` for the ONNX files, or download-on-first-run and cache to disk (like OMP does). For a combined ~300MB of models, embedding in the binary is borderline - download-and-cache is probably better.

### What to fine-tune, what to use off-the-shelf

| Component | Recommendation | Why |
|---|---|---|
| **Classifier encoder** | Start with `MoritzLaurer/deberta-v3-xsmall-zeroshot` (22M), swap to TinyBERT-4 if faster needed | Off-the-shelf zero-shot works; fine-tune later on your signal taxonomy |
| **Classification head** | Train from scratch | 25 signals specific to your pipeline stages |
| **Attention pooling layer** | Train from scratch | ~100K params, learns which chunks matter for your task |
| **Generator** | Fine-tune `SupraLabs/supra-title-50M` or Gemma 270M | Needs to learn your output format (status phrases vs. titles) |

### Training data strategy

Generate 10-20K examples with a frontier model. The input is a prompt + pipeline stage. The output is:
1. 25 signal labels (for the classifier)
2. A natural-sounding status phrase (for the generator)

Key from OMP's findings: **don't use few-shot examples in training prompts for sub-600M models** - it hurts. Use zero-shot system prompt + completion masking.

The [Hugging Face research](2e8445d5-9596-432e-b250-55ea674b71cb) found multiple datasets you could bootstrap from:
- `BananaMind/BananaMind-Chat-Title-200K` (200K title pairs from lmsys-chat-1m)
- `agentlans/wikipedia-paragraph-titles` (paragraph-to-title pairs)
- `JulesBelveze/tldr_news` (article-to-headline pairs)

But you'd want to augment these with progress-phrase-specific data since status text ("Analyzing your configuration...") has a different distribution than titles ("Rust Async Patterns").

### Bottom line

Your architecture - classifier + small conditioned generator - is the right call, and it's more novel than I initially gave it credit for. Nobody else is doing dynamic contextual progress text with local models. The key adjustments from research:

1. **Use decoder-only for generation** (Gemma 270M or SupraTitle-50M), not encoder-decoder (T5)
2. **Use TinyBERT-4 or DeBERTa-v3-xsmall for classification**, not a full BERT
3. **Use the prefill trick** (`<status>` tag + greedy decode) for output control
4. **Use `ort` in Rust** for ONNX inference - the stack is battle-tested
5. **Subsample + attention-pool** for long prompts, not recursive summarization
6. **Skip the custom tokenizer** - pretrained tokenizers + truncation + signal conditioning does the compression job better

**Confidence: high** - every component of this pipeline has production precedent, even if the specific combination is novel.

---

> Oh no, no, I don't want that. I don't want the simple heuristic. I want an actual I wanna do the whole thing. Like, this is the model that I want. I wanna take a I have, first I have my big block of text. The first step is I wanna do maybe something mechanical. Remove any line that's like mechanically retarded, like for example, if it's in a code fence, if it's a code fence, like Lua, we don't need to see Lua. And then Maybe even tables, I don't know, but some fir like some, a first pass to remove stuff so that there's less bulk. And then the next pass is we chunk the whole thing, whatever, five hundred twelve tokens, two, two fifty-six tokens, whatever. We fucking, we, we, we, we tokenize it and then we can, and then we chunk it into specifically token chunks. Then we classify each chunk and we say, does it have signal? Or how much signal does it have? And then, like, we sort. Or something, but I wanna use everything. I have you have to use the whole input, or else it's not gonna be reliable. There's gonna be some cases where it looks stupid, and I don't want that. I wanna do recursive. Like, let's say there's ten, let's say there's like ten chunks. I wanna take pairs. I wanna take the first and the second chunk, and I wanna reduce them, like I wanna summarize them. Then I wanna take the third and the fourth chunk, and like I wanna summarize that. And then I wanna keep doing that until we have the result. And then of course, there's, if there's two chunks, let's say there's two chunks, and, and maybe we can do like a comparison, like, given two chunks. How much stronger is the strong one? And if it's significantly strong, then we get rid of the other one, and that's how we can trim the bulk. A multi-layered approach.

OK. I hear you. Full coverage, no shortcuts, no subsampling. Every token gets processed, classified, and either explicitly kept or explicitly discarded by a model. Let me formalize what you're describing.

## The Algorithm: Binary Tree Reduction with Signal Pruning

```
INPUT: 10,000 token prompt

STEP 0 - Mechanical strip
  Remove: code fence bodies, table bodies, markdown formatting noise
  Keep: prose, instructions, headers, list items
  Cost: <1ms, regex/state machine

STEP 1 - Tokenize & chunk
  BPE tokenize entire stripped text
  Split into fixed 256-token chunks
  → 32 chunks (for a ~8K post-strip prompt)

STEP 2 - Classify all chunks (BATCHED)
  Run classifier on all 32 chunks in one batched forward pass
  Each chunk gets a signal score: 0.0 (pure boilerplate) to 1.0 (core intent)
  → 32 scored chunks

STEP 3 - Pairwise reduction (recursive)

  LEVEL 0: 32 chunks → 16 pairs
    For each pair (A, B):
      if score(A) >> score(B) by threshold: drop B, keep A
      if score(B) >> score(A) by threshold: drop A, keep B
      else: reduce(A, B) → merged chunk (shorter than A+B)
    → ~16 chunks (some trimmed, some merged)
    Reclassify all surviving chunks (BATCHED)

  LEVEL 1: 16 chunks → 8 pairs
    Same pairwise compare-or-merge
    → ~8 chunks
    Reclassify

  LEVEL 2: 8 → 4
  LEVEL 3: 4 → 2
  LEVEL 4: 2 → 1

  → Single distilled chunk containing the essence of the entire input

STEP 4 - Generate status phrase
  Feed final distilled chunk + pipeline stage signals to generator
  → "Analyzing multi-source governance policy comparison"
```

## What the reduce operation actually does

This is the core of the system. When two chunks are paired, three things can happen:

```
Pair (A, B) where A.score=0.82, B.score=0.15
  → B is boilerplate. DROP B. Carry A forward unchanged.

Pair (A, B) where A.score=0.71, B.score=0.68
  → Both have signal. MERGE. Feed both to the reducer model.
  → Output: compressed chunk that preserves the key content of both.
  → Output length: maybe 128 tokens from 512 input tokens (2:1 compression)

Pair (A, B) where A.score=0.12, B.score=0.09
  → Both are noise. COLLAPSE to minimal token: "[low-signal-content]"
  → Almost free compression - these chunks were boilerplate anyway.
```

The threshold for "significantly stronger" is tunable. A tight threshold (0.3 difference) means more merging, better fidelity, slower. A loose threshold (0.1 difference) means more dropping, faster, slightly less reliable.

## The models in this pipeline

You need three models, all small, all running in the same Rust process:

### 1. Signal Classifier (encoder-only)
- **Job**: Score each chunk 0.0-1.0 for "how much intent/action signal is here?"
- **Model**: TinyBERT-4 (14.5M params) with a single linear head
- **Input**: 256 tokens
- **Output**: single float (signal score)
- **Batching**: All chunks at a level processed in one forward pass
- **Latency**: ~5-8ms for batch of 32 on GPU

### 2. Pairwise Reducer (encoder-decoder)
- **Job**: Take two chunks (512 tokens total), produce one compressed chunk (~128 tokens)
- **Model**: T5-efficient-tiny (15.6M params) or T5-small (60.5M), fine-tuned for compression
- **Input**: "[CHUNK_A] {text} [CHUNK_B] {text}"
- **Output**: compressed representation preserving key content from both
- **Batching**: All pairs at a level processed in one forward pass
- **Latency**: ~10-20ms for batch of 16 on GPU

This is where T5 *does* work - compression/summarization is its strength. It failed at titling because titling requires composing a novel phrase. Compression is just extractive + paraphrase, which T5 handles well even at tiny sizes.

### 3. Status Phrase Generator (decoder-only)
- **Job**: Take the final distilled chunk + 25 signals, produce 5-8 word status phrase
- **Model**: Gemma 270M or SupraTitle-50M, fine-tuned
- **Input**: "[ACT:analyze][DOM:policy]... {distilled chunk}"
- **Output**: "Analyzing multi-source governance comparison"
- **Latency**: ~15-25ms single inference

## Honest latency analysis

For a **10K token prompt** (32 chunks after strip):

| Level | Chunks In | Classify | Pairs | Reduce | Level Total |
|---|---|---|---|---|---|
| 0 | 32 | 8ms (batch 32) | 16 | 18ms (batch 16) | ~26ms |
| 1 | ~16 | 6ms (batch 16) | 8 | 14ms (batch 8) | ~20ms |
| 2 | ~8 | 4ms (batch 8) | 4 | 10ms (batch 4) | ~14ms |
| 3 | ~4 | 3ms (batch 4) | 2 | 8ms (batch 2) | ~11ms |
| 4 | ~2 | 2ms (batch 2) | 1 | 6ms (batch 1) | ~8ms |
| Gen | 1 | - | - | 20ms | ~20ms |
| **Total** | | | | | **~99ms** |

For a **2K token prompt** (8 chunks):

| Level | Chunks | Total |
|---|---|---|
| 0 | 8 → 4 | ~14ms |
| 1 | 4 → 2 | ~11ms |
| 2 | 2 → 1 | ~8ms |
| Gen | 1 | ~20ms |
| **Total** | | **~53ms** |

For a **short prompt** (1-2 chunks): ~30ms. For a **massive prompt** (100K tokens, 390 chunks): ~150-180ms with 8-9 reduction levels.

**The 50ms target holds for typical prompts (under ~3K tokens). Longer prompts scale as O(log N) which is about as good as you can get while processing everything.**

## The signal-based pruning is what keeps this fast

Without pruning, every level does full reduction on all pairs - expensive. With pruning:

- Level 0 might start with 32 chunks but 40% are boilerplate (code fences you missed, repeated context, formatting). Pruning drops ~13 chunks instantly.
- Level 1 starts with ~19 chunks instead of 32. Already 40% less work.
- By level 2-3, you're down to the 4-6 chunks that actually matter.

The pruning is *why* you can afford to process everything. You touch every chunk, but you don't waste reduction capacity on noise. The classifier identifies it, the algorithm discards it, and the reduction tree collapses faster.

## Training data for the reducer

This is the novel model in the pipeline. The classifier and generator have off-the-shelf starting points, but the **pairwise reducer** - "take two text chunks and compress them into one while preserving intent" - needs custom training.

Generate training data like this:

```
INPUT:  [CHUNK_A] You are a helpful assistant that analyzes 
        C++ committee papers and identifies voting patterns 
        across working groups.
        [CHUNK_B] Given the following paper D4130 on SD-4 
        corrective measures, compare the voting outcomes 
        in LEWG versus EWG for the period 2024-2026.

TARGET: Analyze C++ committee paper D4130 comparing LEWG 
        and EWG voting patterns on SD-4 correctives 2024-2026.
```

The reducer learns to:
- Keep proper nouns, numbers, specific references
- Drop hedging, politeness, redundant context
- Merge overlapping intent from both chunks
- Produce output ~25-50% the length of input

10-20K examples generated by a frontier model. Fine-tune T5-efficient-tiny or T5-small on these pairs.

## Memory budget on 48GB GPU

| Model | Params | VRAM (Q4) | VRAM (FP16) |
|---|---|---|---|
| Classifier (TinyBERT-4) | 14.5M | ~8MB | ~29MB |
| Reducer (T5-small) | 60.5M | ~30MB | ~121MB |
| Generator (Gemma 270M) | 268M | ~160MB | ~536MB |
| **Total** | 343M | **~198MB** | **~686MB** |

You're using less than 1.5% of your 48GB card. The rest is available for actual inference workloads. These three models are basically free to keep resident.

## The mechanical pre-filter

Don't overthink this. It's a state machine, not a model:

```rust
fn strip_low_signal(text: &str) -> String {
    // State machine that tracks:
    // - Inside code fence? Strip body, keep language tag as "[code:lua]"
    // - Inside table? Collapse to "[table: N rows]"
    // - Inside blockquote? Keep (often contains key content)
    // - Consecutive blank lines? Collapse to one
    // - Markdown image/link syntax? Strip to alt text / link text
    // - HTML tags? Strip entirely
    // - Lines that are only punctuation/formatting? Drop
}
```

This is Rust string processing - microseconds even on huge inputs. The goal isn't semantic understanding, it's just reducing bulk so the classifier doesn't waste capacity on ````python\nimport os\n````.

## Summary

What you're describing is a **full-coverage binary tree reduction with signal-weighted pruning**. It's more thorough than anything in the existing literature for this task. The latency scales as O(N log N) in classify calls and O(N) in reduce calls (because pruning shrinks each level), which keeps it fast. Three small ONNX models totaling ~200MB Q4, all resident on GPU, all running through `ort` in a single Rust process.

The only thing I'd flag: the reducer model is the riskiest component to train. Pairwise text compression with intent preservation is a specific skill that existing small models don't have out of the box. Budget time for a few iterations of training data generation and evaluation. The classifier and generator are comparatively easy. **Confidence: high on the architecture, medium on first-iteration reducer quality.**

---

> What you're saying is, is that we should train the, we should do the riskiest component first.

Yes. Exactly. The reducer is the load-bearing component. If it can't compress two chunks into one while preserving intent, the whole tree collapses into garbage. The classifier and generator are downstream consumers of the reducer's output - if the reducer works, they're easy. If it doesn't, nothing else matters.

Build the reducer first. Validate it. Then build the rest around it.

**Confidence: high** - standard risk management: retire the hardest risk earliest.

---

> what design topics have we discussed, brutal bullets

- Strip code fences, tables, formatting noise mechanically before any model touches it
- Tokenize with BPE, chunk into fixed 256-token blocks
- Classify every chunk for signal strength (0.0-1.0) using TinyBERT-4 (14.5M params), batched
- Pairwise binary tree reduction - pair chunks, compare signal scores, drop the weak one or merge both
- Threshold-based pruning: if one chunk dominates the pair, kill the other, don't waste reduction capacity
- Reducer model (T5-small or T5-efficient-tiny) compresses two chunks into one, preserving intent
- Recursive - repeat pairing until one chunk remains
- 25-signal classifier extracts domain-agnostic action/object/topic signals from the final distilled chunk
- Decoder-only generator (Gemma 270M or SupraTitle-50M) conditioned on signals produces 5-8 word status phrase
- Prefill trick (`<status>` tag + greedy decode) controls output format
- All three models as ONNX, loaded via `ort` crate in a Rust binary
- Total VRAM ~200MB Q4, fits trivially on 48GB card
- MCP server emits `notifications/progress` with the generated status text
- Makes PromptForge tool calls look like native subagents in Cursor/Claude Code
- No API calls, no network, fully local inference
- Reducer is the riskiest component - build and validate it first
- Training data: 10-20K examples generated by frontier model
- Target latency: ~50ms for typical prompts, O(log N) scaling for large ones
- This is novel - nobody else is doing dynamic contextual progress text with local small models

---
