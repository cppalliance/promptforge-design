---
produced: 2026-09-18
title: PromptForge run harness and event record - what to steal from Obelisk, Duroxide, Codex CLI, Starweaver, eventsdb (SQLite event log, deterministic fan-out linearization, replay, live tail)
---

# PromptForge: How to Record and Linearize a Run

Report type: evaluation / review. It judges PromptForge against 5 popular codebases sharing the technique of recording an executor's or agent runtime's events into a per-run SQLite log with deterministic ordering, replay, and live tail behind a product-neutral host library, and prescribes idioms to adopt, in payoff order.

## Executive summary

PromptForge has the best observer seam in the field and the worst record behind it: the run log is a JSONL file with no sequence number, no timestamp, no run id, and no way to tell one fan-out arm from another, while the SQLite engine already in the workspace is used only for grants. Every reference stamps sequence and time in the store rather than the producer, and three of them reach the same two ids (parent chain, per-parent contiguous sequence) to make concurrent children explicit in a flat log. Where PromptForge already wins is the `Observer` write/read split, the drop-guarded exactly-once arm terminal, cursor-based tail with broadcast as a wakeup, and an enforced 500-line ceiling that none of the five references keep. The top finding pays for the whole harness: once the record row is store-sequenced and parent-attributed, the deterministic flat order the Run window needs is a sort key, not a scheduler change.

### Key findings

1. **Sequence, time, and identity are stamped by the store, not the producer (eventsdb, Obelisk, Duroxide, Codex CLI, Starweaver).** Every reference keys its event table on `(run, sequence)` and writes the timestamp and a format version beside the payload. PromptForge's `RuntimeEvent` carries none of these; the harness record must. Confidence: high.
2. **Concurrent children become explicit through a parent id and a per-parent contiguous sequence (Obelisk, Duroxide, Starweaver).** A reader attributes every interleaved row from ids alone, never from arrival timing. This is the plan's `ChainStarted { parent }` plus `chain_seq`, confirmed by three independent designs. Confidence: high.
3. **The recorder is a writer actor that commits first and publishes after (all five).** No lock is held across I/O on the driver path; positions are announced only after the transaction commits. PromptForge's `WorkshopObserver` does the file append under the same lock as the memory append and the broadcast. Confidence: high.
4. **One monotonic committed position is the only wakeup; readers page from their own cursor (eventsdb, Obelisk, Starweaver).** A `watch` of the position cannot lag and carries no payload, so replay and tail are one loop. PromptForge's broadcast of full events with capacity 256 can lag and forces a second recovery path. Confidence: high.
5. **Resolution order equals record order, never completion order (Duroxide, Obelisk).** Joins poll children in index order against completions applied from the ordered record. PromptForge already packs results in collection order; the record must say so. Confidence: medium.
6. **A migration ladder reads the version inside each step's transaction and refuses newer files (eventsdb, Starweaver, Codex CLI, Obelisk).** PromptForge's workspace file already has the versioned stamp; the run record starts as a ladder. Confidence: high.
7. **One store contract suite runs over every backend and ordering properties are regression tests that name their defect (Obelisk, Duroxide, eventsdb, Starweaver).** The plan's Memory and File record targets are the first pair. Confidence: medium.
8. **One core runtime behind one protocol, hosted in-process and over a wire by every client (Codex CLI, Starweaver, Obelisk).** Codex proves it at scale: TUI, exec, and app-server consume the same core through one facade. Confidence: medium.
9. **A reader never fails the whole record on one bad row (Duroxide, Codex CLI, eventsdb).** Decode errors ride in the row; the record stays readable. PromptForge's replay rejects the file on the first malformed line. Confidence: medium.

## Method

The subject was profiled first through nine lenses (module decomposition, state ownership, boundaries, error shape, resource lifecycle, testing, comment policy, build and delivery, messes), producing eight named deficits and five strengths. The field was surveyed with 15 searches over 24 candidates, 14 verified against manifests or source, and 5 shortlisted by technique match with popularity as a tiebreaker. Each reference was cloned at a pinned commit and dived through the same nine lenses in parallel. Every cited idiom then received a provenance tag from commit history; where AI markers touched a cited file, the mechanism at HEAD was compared against the last pre-AI form. Findings were synthesized by convergence and deficit mapping, then every citation in the ranked findings was checked against the pinned clones: 71 of 74 passed as written, 3 were relocated, none dropped.

## Reference projects and provenance

| Reference | Popularity | Why chosen | License | Provenance of cited idioms |
|---|---|---|---|---|
| Obelisk | 734 stars | Rust workflow engine; versioned per-execution SQLite log is the sole truth; join sets linearize children; CLI, gRPC, web UI read the same log | AGPL-3.0-only | 2 strong human signal (join-set seq, dual-backend tests); 4 explicit AI marker, every one present pre-AI and unchanged or tightened; 1 AI-originated (schema JSON derive) |
| Duroxide | 217 stars | Embeddable Rust durable runtime; history table with runtime-assigned ids; fan-out resolved by history order | MIT | 1 strong human signal (schema); 1 unknown (combinators); 6 explicit AI marker, load-bearing lines all from unmarked commits, held up or tightened |
| Codex CLI | 125,099 stars | Production agent runtime; recorder actor, ordinal envelope, one core behind TUI, exec, and app-server | Apache-2.0 | 3 strong human signal (thin); 9 unknown (attribution moved off commit trailers 2026-07); 9 explicit AI marker, cited mechanisms all introduced by unmarked commits |
| Starweaver | 8 stars | Rust 2024 workspace with Tauri desktop, CLI, JSON-RPC host over session, stream, storage crates; nearest product shape | BSD-3-Clause | 10 explicit AI marker, all AI-originated (maintainer's agent bot on 134 of 188 commits); no pre-AI form except a Makefile and AGENTS.md that lacked the cited mechanism |
| eventsdb | 95 downloads | In-process SQLite append-only log; single writer, reader pool, position cursor with replay, subscribe, follow; small enough to read whole | MIT OR Apache-2.0 | 17 strong human signal on the single-maintainer criterion; repository is four days old and documents a coding-agent workflow |

## Baseline: where the subject stands

PromptForge is a Rust 2024 workspace (about 755 Rust files, 155 TypeScript plus 136 test modules, tokio, axum, mlua with vendored Lua, tauri 2, turso pinned at 0.7.2) shipping a gateway via cargo-dist and a Tauri Workshop that embeds the axum server in-process. Its executor exposes one injection point, `RunContext::observer`, whose `Observer` trait separates payload-free lifecycle observations from content hooks with default bodies, and keeps `EventLog` (history as run input) as a distinct trait. Fan-out arms report exactly one terminal event through a drop guard and pack results in collection order through preallocated slots. The Workshop tails a run through a broadcast used only as a wakeup, draining by per-client index from an append-only log. The workspace file is a single-connection turso actor with WAL checkpoint on close and a versioned `meta` stamp.

The deficits the findings map to: the persisted record discards every lifecycle observation and hardcodes `chain_id` and `depth` to zero on content hooks, so arms are indistinguishable (1); `RuntimeEvent` has no sequence, timestamp, or run id, so order is file position and identity is the file name (2); linearization is an accident of the single driver thread and the log takes its write lock across a synchronous file append (3); turso serves grants only and run replay rejects a whole file on one torn line (4); `chain_id` means an arena index in one place, a literal zero in another, and "fanout chain" in the docs (6); live tail and durable log are two channels reconciled by convention on both ends (7); there is no CLI host (8). Deficit 5, the 2,484-line scheduler, is outside this focus and not mapped.

## Detailed findings, ranked by payoff

### Finding 1: Sequence, time, and identity are stamped by the store

Reference mechanism. eventsdb's envelope takes four caller keys (`kind`, a shallow scalar-only `meta`, a deep `data`, a schema version) and stamps three itself (`seq`, `epoch_ms`, store version); envelope keys become columns, `meta` and `data` become TEXT JSON, and any foreign top-level key is refused with a message saying where it belongs ([eventsdb-core/src/event.rs L191-L286](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-core/src/event.rs#L191-L286), [eventsdb-sqlite/src/row.rs L17](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/row.rs#L17)). Obelisk keys its append-only log `PRIMARY KEY (execution_id, version)`, stores the enum tag and join-set id as indexed columns beside the JSON body, and answers a stale appender with `VersionConflict { expected, requested }` ([V1__initial.sql L1-L20](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/migrations/V1__initial.sql#L1-L20), [sqlite_dao.rs L1084-L1100](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L1084-L1100), [sqlite_dao.rs L2305-L2392](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L2305-L2392)). Duroxide's `Event` header carries `event_id`, `source_event_id`, `instance_id`, `execution_id`, `timestamp_ms`, and the runtime version, keyed `(instance, execution, event_id)` so a duplicate id is a constraint violation ([lib.rs L1098-L1125](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/lib.rs#L1098-L1125), [initial_schema.sql L25-L33](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/migrations/20240101000000_initial_schema.sql#L25-L33), [providers/mod.rs L1119-L1127](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/providers/mod.rs#L1119-L1127)). Codex CLI puts an ordinal and an RFC 3339 timestamp on every persisted line and advances the ordinal only after a successful write ([recorder.rs L2039-L2066](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/rollout/src/recorder.rs#L2039-L2066), [ordinal.rs L17-L100](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/rollout/src/ordinal.rs#L17-L100)). Starweaver wraps every durable record in `{schema, version, payload}` with fixtures per version ([protocol.rs L65-L202](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-core/src/protocol.rs#L65-L202), [protocol_contracts.rs L10-L11](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-core/tests/protocol_contracts.rs#L10-L11)).

What it replaces. `RuntimeEvent` in `crates/promptforge-api-types/src/events.rs` (no sequence, timestamp, or execution id) and the JSONL line in `crates/workshop/gateway/src/observer.rs` whose order is file position.

Fix. The harness record's `events` table carries store-stamped `arrival_seq`, `at_ms`, and a format version in `meta`; coordinates (`chain_id`, `chain_seq`, `depth`, `turn`, `arm_index`) are scalar columns; the payload is one JSON column; the producer never chooses a sequence number. Confidence: high, since all five references converge and the subject's turso actor already exists.

### Finding 2: Concurrent children become explicit through a parent id and a per-parent contiguous sequence

Reference mechanism. Obelisk sends child results to a separate append-only `t_join_set_response` table with a per-parent contiguous 1-based `seq` computed as `COALESCE(MAX(seq),0)+1 ... RETURNING seq` under its single writer, while the parent log records only requests and consumption points, so arrival order (`seq`) and consumption order (`version`) are both explicit and replay re-pairs them ([sqlite_dao.rs L2720-L2818](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L2720-L2818), [V13__join_set_response_seq.sql](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/migrations/V13__join_set_response_seq.sql), [storage.rs L302-L323](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/concepts/src/storage.rs#L302-L323)). Duroxide records each fan-out arm as a schedule row with id N and a completion row whose `source_event_id` points at N, so a reader attributes every interleaved event from ids alone ([lib.rs L1102-L1105](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/lib.rs#L1102-L1105), [replay_engine.rs L864-L883](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/runtime/replay_engine.rs#L864-L883)). Starweaver stamps `source {agent_id, task_id, run_id, parent_run_id, source_sequence}` on every record, rebases child records into the parent keeping their own ordinal, and makes duplicate appends no-ops through `PRIMARY KEY (scope, sequence_no)` plus `INSERT OR IGNORE` plus read-back equality ([raw.rs L300-L343](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-stream/src/raw.rs#L300-L343), [raw.rs L387-L461](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-stream/src/raw.rs#L387-L461), [stream_archive.rs L63-L125](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-storage/src/stream_archive.rs#L63-L125)).

What it replaces. `WorkshopObserver::observe` discarding every lifecycle observation in `crates/workshop/gateway/src/observer.rs`, and the hardcoded `chain_id = 0, depth = 0` on content hooks in `crates/promptforge-api-runtime/src/execute/tool_loop.rs`.

Fix. `Observation::ChainStarted { parent }` and `Coordinates { chain_id }` on `observe`; a `chains(chain_id, parent, fork_seq, section)` table; a per-chain `chain_seq`. The canonical order key (parent key, then `fork_seq`, section, chain id, then `chain_seq`) is a pure function of those columns, so two runs with the same replies produce the same order regardless of concurrency. Confidence: high, since three independent designs reach the same two ids and the plan's Step 1 supplies them.

### Finding 3: The recorder is a writer actor that commits first and publishes after

Reference mechanism. eventsdb buffers positions appended inside a transaction in a `TxnContext` and announces them only after `tx.commit()` returns, because waking a subscriber at a rolled-back position walks it past a hole it can never fill ([txn.rs L34-L39](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/txn.rs#L34-L39), [txn.rs L372-L375](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/txn.rs#L372-L375), [hatch.rs L711-L735](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/hatch.rs#L711-L735)); one writer connection plus a `READ_ONLY` and `query_only` reader pool, with the three reads that must stay on the writer named in the docs ([shared.rs L27-L80](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/shared.rs#L27-L80), [log.rs L313-L334](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/log.rs#L313-L334)). Obelisk's single writer thread drains boxed `LogicalTx` closures from a bounded mpsc, applies all in one `IMMEDIATE` transaction, commits once, answers each caller's oneshot, skips and restarts on a failing closure, and fires notifications after commit ([sqlite_dao.rs L309-L333](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L309-L333), [sqlite_dao.rs L683-L831](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L683-L831), [sqlite_dao.rs L913-L966](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L913-L966)). Codex CLI's recorder is a bounded `mpsc(256)` of `AddItems / Persist / Flush / Shutdown / Discard` commands with oneshot acks; the pending queue is drained only by the written prefix, a failed write reopens and retries once, and a terminal failure is stored and returned to later callers ([recorder.rs L126-L141](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/rollout/src/recorder.rs#L126-L141), [recorder.rs L976-L1000](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/rollout/src/recorder.rs#L976-L1000), [recorder.rs L1746-L1947](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/rollout/src/recorder.rs#L1746-L1947)). Duroxide's executor produces the turn's `Vec<Event>` synchronously and one `ack_orchestration_item` persists the delta, enqueues, and releases the lease in a single transaction ([providers/mod.rs L1272-L1313](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/providers/mod.rs#L1272-L1313), [execution.rs L26-L42](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/runtime/execution.rs#L26-L42)). Starweaver runs every rusqlite call inside `blocking::run` with owned inputs and a drop-guard operation tracker so an aborted caller cannot orphan a durable write ([blocking.rs L9-L99](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-storage/src/blocking.rs#L9-L99)).

What it replaces. `WorkshopObserver::append` in `crates/workshop/gateway/src/observer.rs`, which writes the file, pushes to memory, and broadcasts under one write lock on the driver path.

Fix. The harness `Recorder` is a sync `Observer` that pushes onto an unbounded mpsc; the writer task batches rows into one transaction per drain, commits, then publishes the committed position; the in-memory log is updated by the writer, not the observer. Confidence: high, since all five agree and the subject's turso actor is the same shape minus group commit.

### Finding 4: One monotonic committed position is the only wakeup

Reference mechanism. eventsdb makes replay and subscribe one paged loop (`Exhausted::End | Wait`) that holds no statement between pages, reads the head before each batch, advances `cursor = max(cursor, head)` on a short page, and wakes on a `watch<Position>` published via `send_if_modified`, monotonic, unable to lag, never read as payload, with a poll-interval fallback ([log.rs L814-L908](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/log.rs#L814-L908), [shared.rs L82-L102](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/shared.rs#L82-L102), [project.rs L329-L339](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/project.rs#L329-L339)). Obelisk registers a tagged oneshot inside the read transaction only when no rows exist past the cursor; the notification carries no payload and the reader re-reads from the same watermark ([sqlite_dao.rs L337-L342](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L337-L342), [sqlite_dao.rs L6450-L6522](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L6450-L6522)). Starweaver pairs a broadcast wakeup with a `ReplayCatchupSource`, merges backlog by sequence in a `BTreeMap`, refills from the durable store on `Lagged` or `Closed`, and writes durable first then re-appends idempotently to the live log so a crash between the two is repaired by exact retry ([replay.rs L360-L472](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-stream/src/replay.rs#L360-L472), [replay_log.rs L68-L120](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-storage/src/replay_log.rs#L68-L120)).

What it replaces. `broadcast::Sender<RuntimeEvent>` with capacity 256 in `crates/workshop/gateway/src/observer.rs` and the lag recovery in `crates/workshop/sessions/src/agents/socket.rs`.

Fix. `RunHandle::subscribe` yields a `watch::Receiver<u64>` of the committed position; the socket task drains `events_after(cursor)` on each change; the same loop serves replay from zero and live tail, so the frame index and the record cursor are one number. Confidence: high, since the subject already drains by index and only the channel payload and commit ordering change.

### Finding 5: Resolution order equals record order, never completion order

Reference mechanism. Duroxide's `PollAllJoin` polls children in index order into preallocated slots and `Select2` takes the first Ready in argument order, with readiness driven only by completions applied from the ordered record, so resolution order equals record order on every replay ([combinators.rs L9-L61](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/combinators.rs#L9-L61), [combinators.rs L176-L213](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/combinators.rs#L176-L213), [replay_engine.rs L605-L643](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/runtime/replay_engine.rs#L605-L643)). Obelisk's replay re-pairs arrival `seq` with consumption `version`, so both orders survive in the record ([sqlite_dao.rs L2720-L2818](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/db-sqlite/src/sqlite_dao.rs#L2720-L2818), [storage.rs L302-L323](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/concepts/src/storage.rs#L302-L323)).

What it replaces. Nothing in behavior: `complete_arm` and the preallocated slots in `crates/promptforge-api-runtime/src/execute/scheduler.rs` already resolve in collection order. What is missing is the recorded form.

Fix. The canonical order key in the harness (parent prefix, `fork_seq`, section, chain id, `chain_seq`) records what the scheduler does; the pin is one fan-out fixture run at concurrency 1, 2, and 8 with reversed reply order asserting byte-identical ordered output. Confidence: medium, since only two references carry the mechanism and the subject already has the behavior.

### Finding 6: A migration ladder that reads the version inside each step and refuses newer files

Reference mechanism. eventsdb's `LADDER: &[&str]` reads `user_version` inside each step's own `IMMEDIATE` transaction (closing the two-connections-on-a-fresh-file race), keeps steps append-only with the motivating defect documented per step, and refuses newer files and foreign tables at version zero ([schema.rs L184-L293](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/schema.rs#L184-L293)). Starweaver checksums ordered migrations (FNV over SQL plus hook version), validates them on open, and exposes a non-mutating status the host uses as a refuse-to-start gate ([schema.rs L5-L34](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-storage/src/schema.rs#L5-L34), [migrations.rs L83-L215](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-storage/src/migrations.rs#L83-L215)). Codex CLI's migrator sets `ignore_missing` so older binaries open newer databases ([migrations.rs L12-L28](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/state/src/migrations.rs#L12-L28)). Obelisk derives `JsonSchema` on its storage enums, regenerates `assets/schemas/db.json` from a `DbStorageSchema` root, and fails CI when the checked-in schema drifts ([storage.rs L3735-L3743](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/concepts/src/storage.rs#L3735-L3743), [generate.rs L317-L333](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/src/command/generate.rs#L317-L333), [check-schemas.yml L33-L39](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/.github/workflows/check-schemas.yml#L33-L39)).

What it replaces. `SCHEMA_V1` and `validate` in `crates/workshop/workspace/src/workspace_file.rs` and `workspace_file-actor.rs`, which stamp and check a version but have no ladder.

Fix. The harness record's schema is a ladder from the first version, each step in its own transaction, newer files refused with required versus actual; a JSON fixture of the `RunEvent` wire shape pins the vocabulary. Confidence: high, since it extends the subject's existing stamp with no behavior risk.

### Finding 7: One store contract suite over every backend, with ordering as named regression tests

Reference mechanism. Obelisk runs one test body against every backend through a 25-line proc macro adding `#[values(Sqlite, Postgres)]`, plus a seeded differential property test asserting identical sanitized logs from both ([db-macro/src/lib.rs](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/testing/db-macro/src/lib.rs), [diff-tests.rs L41-L120](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/testing/db-tests/tests/diff-tests.rs#L41-L120)). Duroxide's feature-gated provider conformance suite binds per backend in about 100 lines ([provider_validation/mod.rs L156-L204](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/provider_validation/mod.rs#L156-L204), [sqlite_provider_validations.rs](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/tests/sqlite_provider_validations.rs)). eventsdb keeps contention and ordering properties as tests with loose bounds and names the original defect per regression test ([readers.rs L30-L93](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/tests/readers.rs#L30-L93), [two_logs.rs L33-L92](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/tests/two_logs.rs#L33-L92), [regressions.rs](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/tests/regressions.rs)). Starweaver includes one shared contract suite via `#[path]` into both the in-memory and SQLite test binaries ([session_store_contract.rs L8-L9](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-storage/tests/session_store_contract.rs#L8-L9)).

What it replaces. The event-stream fixtures in `crates/promptforge-api-runtime/tests/suite/`, which pin one in-memory observer.

Fix. The harness integration tests run one body against `RecordTarget::Memory` and `RecordTarget::File`; the determinism test is a named regression pin. Confidence: medium, since the subject has no second backend yet and the payoff arrives with the first.

### Finding 8: One core runtime behind one protocol, hosted in-process and over a wire

Reference mechanism. Codex CLI's `CodexThread::submit / next_event` is wrapped by an app-server that runs the same `MessageProcessor` over stdio, WebSocket, or bounded in-memory channels; the client crate interposes a worker task so unread events cannot block responses; both the exec CLI and the TUI consume the core through it ([in_process.rs L1-L39](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/app-server/src/in_process.rs#L1-L39), [app-server-client/src/lib.rs L1-L17](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/app-server-client/src/lib.rs#L1-L17), [exec/src/lib.rs L973-L1235](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/exec/src/lib.rs#L973-L1235), [app_server_session.rs L1-L4](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/tui/src/app_server_session.rs#L1-L4)). Starweaver enforces the tiering with an `xtask` over `cargo metadata`: no path between product crates, a required runtime-to-stream edge, rusqlite only in storage ([architecture.rs L17-L108](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/xtask/src/architecture.rs#L17-L108)). Obelisk's CLI, gRPC server, and web UI all read the same versioned log by cursor ([storage.rs L302-L323](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/concepts/src/storage.rs#L302-L323)).

What it replaces. `crates/workshop/sessions/src/agents/supervisor/effects.rs` as the only run driver, and a Run button that does nothing.

Fix. `harness-api` is the one core; `workshop-runs` hosts it over the socket now, a later CLI hosts it in-process through the same `RunHandle`, and PaperGate follows; the existing `build-xtask` product matrix gains the `harness-api` edges. Confidence: medium, since Codex proves the shape at scale but the subject has one client today.

### Finding 9: A reader never fails the whole record on one bad row

Reference mechanism. Duroxide returns undeserializable history as `Ok(Some(item))` with `history_error` attached so the lease lifecycle stays clean and the record remains readable; the runtime abandons with backoff, then poisons ([providers/mod.rs L294-L303](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/providers/mod.rs#L294-L303), [orchestration.rs L546-L567](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/runtime/dispatchers/orchestration.rs#L546-L567)). Codex CLI skips and counts undecodable lines, seeks to the last byte and appends a newline before any append so a torn tail never glues to the next record, and counts six anomaly kinds during materialization rather than raising ([recorder.rs L1069-L1132](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/rollout/src/recorder.rs#L1069-L1132), [recorder.rs L2020-L2033](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/rollout/src/recorder.rs#L2020-L2033), [thread_history_materialization.rs L22-L310](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/thread-store/src/local/thread_history_materialization.rs#L22-L310)). eventsdb removes the torn-state class altogether by allocating the rowid inside the committing transaction, so positions are dense as read ([schema.rs L29-L69](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/schema.rs#L29-L69), [store.rs L103-L163](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/store.rs#L103-L163)).

What it replaces. `replay` in `crates/workshop/gateway/src/observer.rs`, which rejects the whole file on one malformed line.

Fix. `RunRecord::events_after` decodes the payload per row and yields a row of kind `other` carrying the decode error instead of failing the read; the legacy JSONL loader truncates at the first torn line and records the count. Confidence: medium, since SQLite makes the torn-line class disappear and the residual value is decode drift and legacy files.

## Provenance

Every Starweaver idiom (Findings 1, 2, 3, 4, 6, 7, 8) is AI-originated: 134 of 188 commits carry the maintainer's agent-bot trailer, and no cited file has a pre-AI form, so no rewind could compare; the two files that predate the marker (the Makefile and the agent instructions) lacked the cited architecture check, which the marked commit introduced. Obelisk's storage idioms (Findings 1, 3, 4) were present at their pre-AI form `1ba3a0b9` and stand unchanged or tightened at HEAD; the join-set sequence (Finding 2) and the dual-backend tests (Finding 7) never saw an AI commit; only the `JsonSchema` derive behind the drift gate (Finding 6) is AI-originated, while the CI gate itself predates it. Duroxide's cited files were touched by 33 marked commits, but the load-bearing lines for every finding trace to unmarked commits and held or tightened against pre-AI forms `806599c`, `3e8b4fd`, and `a1c9c84`; the one AI-originated file is an extraction, not a mechanism. Codex CLI's cited mechanisms (Findings 1, 3, 6, 8, 9) were all introduced by unmarked commits after their pre-AI forms, with marked commits contributing renames and crate splits; nine files are `unknown` because the repository moved attribution off commit trailers in July 2026. eventsdb has no markers on 113 commits by one author over four days; its tag rests on the single-maintainer criterion alone, and the repository documents a coding-agent workflow. The evidence is per idiom; a sample of five projects carries no conclusion about AI-authored code in general.

## Where the subject already matches or beats the references

- The report-only `Observer` with a `#[non_exhaustive]` `Observation` and default-body content hooks, kept separate from `EventLog` as run input, is cleaner than any reference; Duroxide and Obelisk conflate observation and history by design because the history is their replay input.
- Exactly-once terminal event per fan-out arm through a drop guard, with results packed in collection order, matches Duroxide's id-paired completions and eventsdb's Drop-time `detach_append`.
- Broadcast as a wakeup with drain by per-client index beats Obelisk's 200 ms `t_state` polling for external readers ([grpc_server.rs L1456-L1500](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/src/server/grpc_server.rs#L1456-L1500)) and Duroxide's exponential-backoff full-history polling ([client/mod.rs L660-L702](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/client/mod.rs#L660-L702)); it matches eventsdb and Starweaver.
- The single-connection turso actor with WAL checkpoint on close and versioned `meta` validation matches Obelisk's single writer thread and eventsdb's single writer; it lacks only group commit (Finding 3).
- The enforced 500-line ceiling beats every reference at the file level: Obelisk 7,014, Duroxide 4,111, Codex 13,033 against its own stated 500/800 rule, Starweaver 6,635, eventsdb 1,046; the subject's own runtime family is exempt from the rule, which is deficit 5 and outside this focus.

## Messes we should explicitly not copy

- Obelisk: `src/command/server.rs` at 7,014 lines and `crates/db-sqlite/src/sqlite_dao.rs` at 6,743 with `postgres_dao.rs` a hand-copied duplicate of the same trait; 200 ms polling for the gRPC live tail ([grpc_server.rs L1456-L1500](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/src/server/grpc_server.rs#L1456-L1500)); `From<FatalError>` and `From<&FatalError>` as a 100-line verbatim copy ([worker.rs L153-L253](https://github.com/obeli-sk/obelisk/blob/19df53dc67fe488daac9971e0e8f213ff2c851d8/crates/executor/src/worker.rs#L153-L253)).
- Duroxide: `src/providers/sqlite.rs` at 4,111, `src/lib.rs` at 3,767, and `src/providers/mod.rs` at 2,708 lines each carrying four or more roles; every sqlx error in the ack path labelled `"list_instances"`; retryability substring-matched on `e.to_string()` in two places with different substring sets; two schema sources plus a third in a design doc; live tail by exponential-backoff polling of a full-history read ([client/mod.rs L660-L702](https://github.com/microsoft/duroxide/blob/6a458861763a7aa5b78a7c1c97691a6f00489a8b/src/client/mod.rs#L660-L702)).
- Codex CLI: 194 non-test files over 1,000 lines against a stated 500/800 rule, `codex-rs/tui/src/bottom_pane/chat_composer.rs` at 13,033; `Session::send_event` emitting every event twice, typed plus legacy ([session/mod.rs L2363-L2373](https://github.com/openai/codex/blob/7498521d288b9b3b96ffba4eedf089d8d6e06a84/codex-rs/core/src/session/mod.rs#L2363-L2373)); errors flattened to strings at the store boundary; `LiveThread` downcasting `dyn ThreadStore` to the concrete local store.
- Starweaver: `crates/starweaver-rpc/src/service.rs` at 6,635 and `coordinator.rs` at 5,971 lines; an in-memory reference store (3,614 lines) larger than the SQLite one; superseded first-migration tables never dropped; workspace lints negated by a 17-lint `#![allow]` blanket ([storage/src/lib.rs L1-L18](https://github.com/Wh1isper/starweaver/blob/89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687/crates/starweaver-storage/src/lib.rs#L1-L18)); errors flattened via `to_string()` into `Failed(String)`; two SQLite paths bypassing the stated `blocking::run` rule.
- eventsdb: `crates/eventsdb-sqlite/src/log.rs` at 1,046 lines with seven roles; `head_position` running on the writer against the README's own rule ([log.rs L926-L943](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/log.rs#L926-L943) versus [log.rs L741-L750](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/log.rs#L741-L750)); the write body copied six times in `store.rs` and three in `txn.rs` while `append_stamped_now` claims to be the shared body ([store.rs L214-L216](https://github.com/ynishi/eventsdb/blob/2821a64adbc9bfb6995b8c159de01c1c36470537/crates/eventsdb-sqlite/src/store.rs#L214-L216)).

## Recommended execution order

1. Land the coordinate and parent-chain change on the `Observer` (Finding 2, runtime half): `Coordinates { chain_id }` on `observe`, `Observation::ChainStarted { parent }`, real chain id in the tool loop. Everything below depends on it.
2. Create the harness record with the sequenced row, the `chains` table, and the migration ladder in one step (Findings 1, 2, 6): they define the same schema and move the same files.
3. Build the recorder as a writer actor with commit-then-publish and a `watch` of the committed position (Findings 3, 4): one actor, one channel, one publish point.
4. Add the tolerant row reader (Finding 9) beside the record's read API.
5. Write the contract suite over Memory and File targets and the determinism regression pin (Findings 5, 7): the tests that hold the invariants above.
6. Host the harness from `workshop-runs` over the socket, and record the in-process host path for the CLI (Finding 8).

## Refactor notes

- Findings 1, 2, 3, 4, 6, 9 are new structure in a new crate and change no existing behavior except the `Observer` signature, which is one parameter and one added variant; every in-tree implementor is updated in the same change.
- Finding 5 changes no behavior; it records what the scheduler already does. Finding 7 is tests only. Finding 8 adds a client and touches no runtime code.
- The existing test suite is the invariant and moves last: the runtime fixture suites, the workshop socket tests, and the SPA test harness must stay green at every step; the pinned "Run button does nothing" test is the only test replaced, and only when the button gains behavior.
- Do not touch: the scheduler's fan-out join and arm finalizer, the `EventLog` trait, `WorkshopObserver` and the agent-session path (their migration onto the harness is a later slice), the gateway crates, and anything under the private PromptForge family beyond the one door.
- Per step: run the crate's tests, the structural harness, clippy, and fmt; commit once green.
- Stop condition: two consecutive failures on one step stop the run for a re-plan.

## Sources

- Obelisk: https://github.com/obeli-sk/obelisk - HEAD `19df53dc67fe488daac9971e0e8f213ff2c851d8` - AGPL-3.0-only - analyzed 2026-09-18 - PRE_AI `1ba3a0b9b0529beed19c43e954cfe0382403b96f` (sqlite_dao.rs), `0e33fb75644a62e36f84f3002b6415eedd074b4c` (storage.rs)
- Duroxide: https://github.com/microsoft/duroxide - HEAD `6a458861763a7aa5b78a7c1c97691a6f00489a8b` - MIT - analyzed 2026-09-18 - PRE_AI `806599c` (lib.rs, sqlite.rs), `3e8b4fd` (providers/mod.rs), `c172db3` (replay_engine.rs), `60d6514` (execution.rs), `a1c9c84` (provider_validation)
- Codex CLI: https://github.com/openai/codex - HEAD `7498521d288b9b3b96ffba4eedf089d8d6e06a84` - Apache-2.0 - analyzed 2026-09-18 - PRE_AI `bbd237348d` (recorder.rs), `3878c3dc7c` (migrations.rs), `69c3d12274` (in_process.rs), `86982ca1f9` (app-server-client), `6ef658a9f9` (exec), `01df50cf42` (tui session), `77b0c75267` (policy.rs)
- Starweaver: https://github.com/Wh1isper/starweaver - HEAD `89e0d158f2c939f5b9bcf03cbfe90b11ba6d6687` - BSD-3-Clause - analyzed 2026-09-18 - PRE_AI `f03ccfe8815d9c3c85033213038e1d245a2e05d3` (Makefile), `54328bb330e11936c6cc615a4b52a13983131229` (AGENTS.md); all other cited files AI-originated
- eventsdb: https://github.com/ynishi/eventsdb - HEAD `2821a64adbc9bfb6995b8c159de01c1c36470537` - MIT OR Apache-2.0 - analyzed 2026-09-18 - no rewinds
- Field survey: 2026-09-18, 24 candidates, 14 verified. Subject profile: 2026-09-18.

*2026-09-18 09:35 - Claude Fable 5.1*

