# Workshop: How to Link to Any Artifact

Report type: evaluation / review. It judges the Workshop (Tauri shell, axum server, TypeScript SPA) against 6 popular codebases sharing the technique of custom URL schemes and internal artifact links, and prescribes idioms to adopt, in payoff order.

## Executive summary

The Workshop has no way to name anything it owns: a panel id is a private `type:param` string, a rendered link navigates the webview away, a second launch throws its arguments on the floor, and no route serves an object by identity. Six references that solved this, from VS Code's 2015 `URI` to Zotero's 2007 protocol handler, converge on one shape: a single address value type with one parser, an opener registry that hands an address to the view that claims it, a delegated click handler that never lets an anchor navigate, a label service so no raw id is ever displayed, a ready-gated intake for links arriving from outside, and id-keyed routes for content. The Workshop already has the two things the references struggled with, a type-keyed slot registry with lifetime guards and a fixture-pinned protocol, so the top finding pays by giving those a grammar to carry. Where it wins: nothing in the field pins two parsers to one fixture the way the Workshop's protocol already does.

### Key findings

1. **One address value type, one parser, one serializer, pinned by a round-trip fixture, from VS Code and Trilium with Zotero and Zed corroborating.** Parse failure is a value, never an exception at the click boundary; the list of kinds lives in one registry. Confidence: high.
2. **An opener registry with priority or order, disposable registration, one entry function, and a handler kind that says navigate-versus-content, from Theia and Zotero.** Replaces 11 direct `openInZone` call sites. Confidence: high.
3. **Neuter every rendered anchor and route one delegated click through the opener, from VS Code and Trilium.** `href` becomes `data-href` at render; internal-versus-external is decided by the parser; a modifier table picks same pane, new tab, or new split. Confidence: high.
4. **Deep-link intake as one channel with a ready-gated queue and a URL firewall, from VS Code, Theia, and Element.** Every inbound source enters the same intake; the URL is parsed and rebuilt, never forwarded; the queue holds until the window is ready. Confidence: high on the shape, medium on the Tauri specifics.
5. **Display names from a kind-keyed label service with priority, a degrade chain, and a change event, from Theia and VS Code.** Resolved name, then noun plus id, then a generic; a change event re-labels tabs. Confidence: high.
6. **Virtual resources keyed by id with a presentational filename tail, served by providers that refuse duplicate registration, from Trilium and VS Code.** The last segment names the download and the language, never the lookup; `no-store` on every response. Confidence: high on routes, medium on streaming bodies.
7. **Panel identity derived from the address by one function, fragments stripped, the key round-tripping through layout persistence, from Theia.** `panelIdFor` becomes a function of the address for every artifact-backed panel. Confidence: medium.

## Method

The subject was profiled first through nine lenses (module decomposition, state ownership, boundaries, error shape, resource lifecycle, testing, comment policy, build and delivery, messes), producing a fingerprint with six focus deficits and five strengths. The field was surveyed stack-agnostically for the technique, seventeen candidates were verified against source, and six were shortlisted. Six parallel dives read each reference at a pinned commit through the host's raw content endpoints rather than local clones, which the run environment could not create; line numbers therefore come from fetched content. Provenance examiners walked every cited file's commit history through the host's commits API, which does not follow renames, so histories truncated at tree moves are tagged unknown rather than guessed; where an AI-marked commit touched a cited file, the file was read at its last pre-AI commit and compared. Synthesis clustered idioms by convergence and mapped each onto a named deficit. A citation check re-fetched 97 citations at the pinned commits: 84 passed as written, 13 were corrected to adjacent ranges in the same files with the mechanism present, and none were unverifiable. Fourteen supporting file-level citations without line numbers (bundle manifests, desktop entries, installer scripts, builder configs) were not independently re-fetched under the check's budget and are marked as such where cited.

## Reference projects and provenance

| Reference | Popularity | Why chosen | License | Provenance of cited idioms |
|---|---|---|---|---|
| VS Code | 192.6k stars | All three mechanisms: `URI` value type, opener service with validators, URL service with a ready-gated Electron listener, scheme-keyed file providers, label formatters | MIT | 10 strong human signal (2015 to 2019 origins); 4 explicit AI marker (markdown renderer held up and slightly tightened; label formatters' scheme interface pre-AI and unchanged, home-template path AI-originated and not recommended); 4 unknown (not walked) |
| Zed | 90.3k stars | Rust; `zed://` grammar parsed into a typed enum, one intake for OS, CLI, and in-app sources, three-layer scheme registration | GPL-3.0-or-later | 9 explicit AI marker, every cited mechanism present at the pre-AI form and unchanged or tightened by later human-marked commits; 1 strong human signal; 3 unknown (extractions and renames) |
| Zotero | 15.3k stars | `zotero://` host-keyed handler table with a navigate-versus-content flag, declarative route table, generated virtual resources, three-platform registration through one CLI flag | AGPL-3.0 | 9 strong human signal (2007, 2009, 2014 origins, one maintainer); 2 unknown; no AI-marked commit touches a cited file |
| Theia | 21.7k stars | Priority-scored `OpenHandler`, JSON widget identity from URI, `LabelProviderContribution`, delegated markdown link handler, Electron deep link through the same opener | EPL-2.0 OR GPL-2.0 w/ CPE | 10 strong human signal (2017 to 2020 origins); 2 unknown; no AI-marked commit touches a cited file |
| Trilium | 37.9k stars | Note-path grammar with serializer, parser, and round-trip tests in one module; document-level anchor dispatcher with a modifier table; id-keyed image routes; link titles with bracketed fallbacks | AGPL-3.0-only | 8 explicit AI marker (Claude trailers from 2026-06), every cited mechanism present at the pre-AI form, one AI-originated trust gate not recommended; 1 strong human signal |
| Element (web and desktop) | 13.5k / 1.5k stars | Pluggable permalink constructors over one `PermalinkParts` type, identity-sentinel href transform, deep-link firewall forwarding only search and hash, per-variant scheme injection | AGPL-3.0-only OR GPL-3.0-only OR commercial | 1 strong human signal; 13 unknown (2026 tree moves truncate rename-blind histories; zero AI markers in reachable commits) |

## Baseline: where the subject stands

The Workshop is a Tauri 2 binary hosting an axum server in-process on an OS-assigned loopback port, with a code-split TypeScript SPA embedded by rust-embed and served to one webview under an exact-origin capability built at runtime. Roughly a third of its source is TypeScript, a third tests, a third Rust split across the server, the shell, a protocol crate, and a registry crate.

Strengths the findings build on: a type-keyed slot registry with RAII registration guards and boot-time `require`; a twin-pinned protocol whose Rust and TypeScript sides share one JSON fixture and classify every frame as durable or ephemeral; a runtime exact-origin capability with a tested navigation policy; a metadata-eager, code-lazy panel registry with reversible ids; and a validated HTTP boundary with code-matched errors.

Deficits the findings map to: (1) no internal URI grammar, identity is the dock-private `panelIdFor` string; (2) no opener or resolver registry, eleven call sites open panels directly; (3) rendered markdown emits plain anchors and nothing intercepts clicks, so a same-origin link navigates the SPA away; (4) no OS deep link, the single-instance callback discards its arguments; (5) no virtual resources over HTTP, every route addresses a bundle asset or a raw OS path; (6) display names know only a file's basename or a static type title.

## Detailed findings, ranked by payoff

### Finding 1: One address value type, one parser, one serializer, pinned by a round-trip fixture

Every reference holds its address in a single immutable value and parses it in exactly one place. VS Code's `URI` is a five-part value with RFC-cited validation ([uri.ts L22-32](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/common/uri.ts#L22-L32), [L102-146](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/common/uri.ts#L102-L146)), a `parse` for URIs and a separate `file` for OS paths so a `#` or `?` in a filename cannot corrupt the grammar ([L271-332](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/common/uri.ts#L271-L332)), a `with()` for derivation ([L218-261](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/common/uri.ts#L218-L261)), and one `Schemas` namespace as the sole list of internal scheme names ([network.ts L12-186](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/common/network.ts#L12-L186)). Zed parses `zed://...` into a typed `OpenRequestKind` enum ([open_listener.rs L37-83](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/zed/open_listener.rs#L37-L83), parse at [L135](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/zed/open_listener.rs#L135)) from a transport-free `RawOpenRequest` ([L383](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/zed/open_listener.rs#L383)). Zotero declares its grammar as a route table, `router.add('library/items/:objectKey', ...)`, with deprecated forms as extra routes that rewrite themselves ([ZoteroProtocolHandler.mjs L573-612](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L573-L612), [L797-813](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L797-L813)), and keeps a canonical object URI separate from action URLs with one regex to reverse it ([uri.js L39-43](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/xpcom/uri.js#L39-L43), [L223-250](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/xpcom/uri.js#L223-L250)). Trilium puts serializer, parser, and round-trip tests in one module: `calculateHash` elides defaults, `parseNavigationStateFromUrl` validates against `NOTE_PATH_PATTERN` and returns `{}` on any failure ([link.ts L113-122](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/link.ts#L113-L122), [L251-299](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/link.ts#L251-L299), [L335-431](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/link.ts#L335-L431); tests [link.spec.ts L74-390](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/link.spec.ts#L74-L390)). Element's `parsePermalink` dispatches to one constructor per grammar and collapses any throw to `null` ([PermalinkConstructor.ts L13-64](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/web/src/utils/permalinks/PermalinkConstructor.ts#L13-L64), [Permalinks.ts L438-463](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/web/src/utils/permalinks/Permalinks.ts#L438-L463)).

What it replaces: `panelIdFor` in the SPA's `ui/layout/zones.ts`, a dock-private `type:param` string with no parser, and the session, event, and wait ids in `workshop-protocol/src/agent.rs` that nothing can address.

The fix: an `ArtifactPath` value type in `workshop-protocol` with `parse`, `Display`, and serde-as-string; a TypeScript mirror; one shared fixture both sides run. Three rules the field agrees on: parsing failure at a click boundary is a value, not a throw; serializer and parser live in one module beside their round-trip tests; the list of kinds is owned by the provider registry and repeated nowhere else.

Confidence: high, six of six references converge and the strong-human forms are the oldest code in the sample.

### Finding 2: An opener registry with priority or order, disposable registration, one entry function, and a navigate-versus-content flag

Theia's `OpenHandler.canHandle(uri)` returns a number; handlers are filtered to `> 0` and sorted, a throw scores zero, `addHandler` returns a `Disposable`, and one `open(service, uri)` is the entry ([opener-service.ts L29-56](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/opener-service.ts#L29-L56), [L95-98](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/opener-service.ts#L95-L98), [L112-166](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/opener-service.ts#L112-L166); [prioritizeable.ts L44-57](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/common/prioritizeable.ts#L44-L57)). VS Code's opener is a chain of responsibility: `registerOpener` unshifts so newest wins, validators run first, openers are walked until one returns true, and `extractSelection` lifts `#L73,84` fragments into selections ([opener.ts L63-124](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/opener/common/opener.ts#L63-L124), [L147-160](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/opener/common/opener.ts#L147-L160); [openerService.ts L161-169](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/editor/browser/services/openerService.ts#L161-L169), [L190-225](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/editor/browser/services/openerService.ts#L190-L225)). Zotero keys handlers by host in an `_extensions` table, gives an unknown host a pre-cancelled channel instead of an error page, and has each handler declare `noContent: true` with a `doAction` when it navigates versus `newChannel` when it serves content, a flag `loadURI` checks before deciding what to do ([ZoteroProtocolHandler.mjs L887-894](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L887-L894), [L922-935](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L922-L935), [L959-962](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L959-L962), [L555-557](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L555-L557), [L780-783](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L780-L783); [zoteroPane.js L5067-5076](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/zoteroPane.js#L5067-L5076)). Zed's `handle_open_request` is the one kind-to-view match ([main.rs L1008-1300](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/main.rs#L1008-L1300)).

What it replaces: eleven direct `openInZone(type, params)` calls in `main.ts`, `ui/layout/workshop-panel.ts`, `ui/workspace/file-actions.ts`, `ui/editor/editor-lifecycle.ts`, and three contribution files.

The fix: an `artifact-openers.ts` registration singleton with `registerArtifactOpener` returning a disposable, one `openArtifact(text)` entry, and an opener that declares whether it navigates (Zotero's flag) so the fallthrough to a read-only editor is a declared default, not a guess from the path shape. The server side has the same shape already in its slot registry.

Confidence: high, every reference with a registry uses this shape; Theia's is the cleanest single-file lift.

### Finding 3: Neuter every rendered anchor and route one delegated click through the opener

VS Code rewrites every rendered `href` into `data-href` and drops `data:`, `javascript:`, and untrusted `command:` links at render ([markdownRenderer.ts L458-483](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/browser/markdownRenderer.ts#L458-L483), unwrap at [L462-467](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/browser/markdownRenderer.ts#L462-L467)), then one delegated `click`, `auxclick`, and `keydown` listener on the render root finds `a[data-href]` and always prevents default ([L308-327](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/browser/markdownRenderer.ts#L308-L327), [L551-571](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/base/browser/markdownRenderer.ts#L551-L571)); the platform layer calls `openerService.open(link, { fromUserGesture, allowCommands })` with trust derived from the source ([platform markdownRenderer.ts L89-101](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/markdown/browser/markdownRenderer.ts#L89-L101)). Theia's link handler does the same with a `data-theia-markdown-links-wired` marker to prevent double wiring ([markdown-link-handler.ts L28-32](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/markdown-rendering/markdown-link-handler.ts#L28-L32), [L47-82](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/markdown-rendering/markdown-link-handler.ts#L47-L82)). Trilium's `goToLinkExt` decides internal-versus-external by whether the parser returned a note path, then dispatches by a modifier table: ctrl, double-click, middle, or `target=_blank` to a new tab, shift to a new window, else the same pane; opting out is a CSS class rather than `stopPropagation` ([link.ts L489-590](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/link.ts#L489-L590), [L740-761](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/link.ts#L740-L761)); external links pass a derived allowlist with a one-line reason per exclusion ([shared_constants.ts L16-42](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/packages/commons/src/lib/shared_constants.ts#L16-L42)). Element's `tryTransformPermalinkToLocalHref` returns its input unchanged for external links, and the sanitizer drops `target=_blank` exactly when the transform changed something ([Permalinks.ts L366-405](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/web/src/utils/permalinks/Permalinks.ts#L366-L405), [Linkify.ts L43-61](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/web/src/Linkify.ts#L43-L61), [TextualBodyViewModel.tsx L277-293](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/web/src/viewmodels/room/timeline/event-tile/body/TextualBodyViewModel.tsx#L277-L293)).

What it replaces: the SPA's `ui/agent/markdown-render.ts` emits plain anchors and nothing intercepts clicks, while the shell's `navigation.rs` allows same-origin hrefs to navigate the webview away from the app.

The fix: rewrite `href` to `data-href` at render so a missed listener cannot navigate; one delegated root listener that always prevents default and hands the address to `openArtifact`; a modifier table for same pane, new tab, and new split, since the dock already has zones and splits; and the external allowlist as one derived constant beside the existing navigation policy.

Confidence: high, four references share the delegated-root shape and the two strong-human forms predate the AI-era edits that left them intact.

### Finding 4: Deep-link intake as one channel with a ready-gated queue and a URL firewall

VS Code registers `setAsDefaultProtocolClient(urlProtocol, execPath, ['--open-url', '--'])` ([electronUrlListener.ts L51-58](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/url/electron-main/electronUrlListener.ts#L51-L58)), takes URLs from macOS `open-url` and from argv elsewhere ([L60-76](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/url/electron-main/electronUrlListener.ts#L60-L76), [L44-49](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/url/electron-main/electronUrlListener.ts#L44-L49)), and holds them in a queue that flushes once a window is ready and re-queues with ten bounded retries ([L79-91](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/url/electron-main/electronUrlListener.ts#L79-L91), [L102-130](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/url/electron-main/electronUrlListener.ts#L102-L130)); handlers implement one `IURLHandler` interface ([url.ts L25-43](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/url/common/url.ts#L25-L43)) and the renderer's service is both a handler and an opener for the product scheme ([urlService.ts L39-66](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/workbench/services/url/electron-browser/urlService.ts#L39-L66), not independently re-fetched). Theia registers a configured scheme, reads `originalArgv` on `second-instance`, holds a first-launch URL until the renderer reports ready, and has the renderer resolve through the same opener service and return accept or refuse so main can try the next window ([electron-main-application.ts L814-822](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/electron-main/electron-main-application.ts#L814-L822), [L829-835](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/electron-main/electron-main-application.ts#L829-L835), [L341-354](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/electron-main/electron-main-application.ts#L341-L354); [electron-uri-handler.ts L28-40](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/electron-browser/electron-uri-handler.ts#L28-L40)). Zed funnels the macOS delegate, argv, a Linux datagram socket, a Windows named pipe, an in-app action, and an editor click into one `OpenListener` ([open_listener.rs L383](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/zed/open_listener.rs#L383)), tests the first queued request to choose restore-versus-open ([L125-133](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/zed/open_listener.rs#L125-L133)), waits on `select_biased!(restore_finished, first_window_placed)` before dispatching ([main.rs L929-954](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/main.rs#L929-L954), [L986-1004](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/main.rs#L986-L1004)), and registers the scheme at bundle time in every bundle block and at runtime through a platform action with a toast ([zed.rs L1271-1296](https://github.com/zed-industries/zed/blob/87f65de697bda93ad0a13dc92eabe941115b61de/crates/zed/src/zed.rs#L1271-L1296); bundle manifest and desktop entry not independently re-fetched). Zotero registers on three platforms and funnels second launches through one `-url` flag to the running window ([xpcom/commandLineHandler.js L39-43](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/xpcom/commandLineHandler.js#L39-L43); plist, desktop entry, and installer script not independently re-fetched). Element's desktop handler validates the scheme, builds a fresh internal URL, copies only `search` and `hash`, and drops the path on purpose ([protocol.ts L75-100](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/desktop/src/protocol.ts#L75-L100), [L124-141](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/desktop/src/protocol.ts#L124-L141)), registers with a filtered argument list ([args.ts L25-40](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/desktop/src/args.ts#L25-L40)), handles cold start only after the single-instance lock and the window exist ([electron-main.ts L106-110](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/desktop/src/electron-main.ts#L106-L110), [L288-292](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/desktop/src/electron-main.ts#L288-L292)), and gives stable and nightly distinct schemes injected at build ([build-config.ts L29-37](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/desktop/src/build-config.ts#L29-L37); builder config not independently re-fetched).

What it replaces: the shell's single-instance callback in `main.rs` discards `_args`; `tauri.conf.json` registers no scheme; the only inbound path is a file drop.

The fix: one intake on the server (`ServerHandle::open_artifact`) that every source uses, including the single-instance argv path today and the scheme handler later; a firewall that parses to the address type and re-serializes, forwarding nothing the grammar did not accept; a hold on the resulting socket frame until the workbench socket is connected, with bounded retry; and a distinct scheme per build variant so a development build never captures the installed one's links.

Confidence: high on the shape, medium on the Tauri specifics, since every reference is Electron or native and Tauri's deep-link plugin will place the pieces differently.

### Finding 5: Display names from a kind-keyed label service with priority, a degrade chain, and a change event

Theia's `LabelProviderContribution` uses the same `canHandle` priority as its opener, has a default URI contribution at priority 1, exposes `getName`, `getLongName`, `getIcon`, and an `onDidChange` whose `affects(element)` drives tab re-labeling ([label-provider.ts L46-53](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/label-provider.ts#L46-L53), [L124-128](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/label-provider.ts#L124-L128), [L347-383](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/label-provider.ts#L347-L383); consumer [editor-widget-factory.ts L55-60](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/editor/src/browser/editor-widget-factory.ts#L55-L60), not independently re-fetched). VS Code's `registerFormatter({ scheme, authority?, formatting })` selects by scheme, then longest authority, then priority, with a per-scheme change event ([label.ts L14-43](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/label/common/label.ts#L14-L43), [L55-94](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/label/common/label.ts#L55-L94); [labelService.ts L271-295](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/workbench/services/label/common/labelService.ts#L271-L295)). Trilium computes a link's title from the parsed target with view-mode variants and renders `[missing note]` rather than a raw id ([link.ts L142-249](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/link.ts#L142-L249), [L687-738](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/link.ts#L687-L738)), optionally prefixing the branch and appending the ancestor path ([tree.ts L202-225](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/tree.ts#L202-L225), path components from [L227](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/apps/client/src/services/tree.ts#L227)). Element's `usePermalink` degrades resolved name to raw id to a generic, and its pill is an anchor only when a URL exists, with the raw id in a tooltip ([usePermalink.ts L87-114](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/web/src/hooks/usePermalink.ts#L87-L114), [L141-163](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/web/src/hooks/usePermalink.ts#L141-L163); [Pill.tsx L158-175](https://github.com/element-hq/element-web/blob/26771fd3b1576f8b35928ce8bba33a2ab6e7def4/apps/web/src/components/views/elements/Pill.tsx#L158-L175)). Zotero stores a fallback label beside the URI inside a note so links render without resolution, and falls back through merge relations when the object moved ([editorInstance.js L1716-1732](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/xpcom/editorInstance.js#L1716-L1732), [L1510-1533](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/xpcom/editorInstance.js#L1510-L1533)).

What it replaces: `titleFor` in `ui/layout/zones.ts` knows a basename or a static type title.

The fix: a noun table keyed by kind with a computed default so every address has a name before its provider exists; a `display_name` function that degrades from a provider-supplied name through noun plus id to the default noun, never an empty string or a raw identifier; and a change event on the opener registry so a tab re-labels when its provider learns a better name. Do not adopt VS Code's home-template formatter: it landed in the last three weeks with two same-day fixes on the analysis date.

Confidence: high, the two strong-human services agree on priority-selected contributions and the degrade chain appears in all five.

### Finding 6: Virtual resources keyed by id with a presentational filename tail, served by providers that refuse duplicate registration

Trilium serves `/api/images/:noteId/:filename` and `/api/attachments/:attachmentId/image/:filename` from one route table injected into three hosts ([routes/index.ts L124](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/packages/trilium-core/src/routes/index.ts#L124), [L158-159](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/packages/trilium-core/src/routes/index.ts#L158-L159), [L335-344](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/packages/trilium-core/src/routes/index.ts#L335-L344)); the handler keys on the id alone, sets the media type from the stored entity, sends `no-store`, and answers a typed 400 on a role mismatch ([image.ts L14-17](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/packages/trilium-core/src/routes/api/image.ts#L14-L17), [L44-45](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/packages/trilium-core/src/routes/api/image.ts#L44-L45), [L87-102](https://github.com/TriliumNext/Trilium/blob/1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0/packages/trilium-core/src/routes/api/image.ts#L87-L102)). VS Code's `registerProvider(scheme, provider)` throws on a duplicate scheme, returns a disposable, and lets a lazy provider `join(promise)` on an activation event so an early request waits instead of failing ([fileService.ts L52-125](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/files/common/fileService.ts#L52-L125); capability bitfield in `files.ts`, not independently re-fetched). Zotero's `AsyncChannel` accepts a string, an async stream, a file, a URI, or nothing, and its `report` host renders HTML and proxies its own CSS within the scheme ([ZoteroProtocolHandler.mjs L1100-1164](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L1100-L1164), [L160-162](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L160-L162), [L190-198](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L190-L198), [L530-537](https://github.com/zotero/zotero/blob/c30163ed907bd17dd1ed66cc63eaaccad6471e5b/chrome/content/zotero/ZoteroProtocolHandler.mjs#L530-L537)). Theia's `ResourceResolver.resolve(uri)` carries a numeric priority and owns one scheme each, with `user-storage:/user` rebased onto the config directory through a two-way converter ([resource.ts L181-224](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/common/resource.ts#L181-L224), [L313-343](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/common/resource.ts#L313-L343), [L346-400](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/common/resource.ts#L346-L400); [user-storage-contribution.ts L35-57](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/userstorage/src/browser/user-storage-contribution.ts#L35-L57)).

What it replaces: every server route addresses a bundle asset or a raw OS path passed as a query parameter; nothing serves an object by identity.

The fix: a `GET /_promptforge/{*path}` dispatcher over an `ArtifactProvider` slot, with four rules from the field: registering a provider for an already-registered kind is an error, never a shadow; a provider keys on id and facet, and the last segment under a file facet is presentational only; `Cache-Control: no-store` on every artifact response; and a body type that can be text, bytes, or a stream so a large event log is not buffered.

Confidence: high on the route shape, medium on the streaming body, which only one reference shows.

### Finding 7: Panel identity derived from the address by one function, fragments stripped, the key round-tripping through layout persistence

Theia's widget identity is `stableJsonStringify({ factoryId, options })`, with `createWidgetOptions(uri)` as the single URI-to-identity step, a pending-promise map that dedups in-flight creation, and the same key stored by layout persistence ([widget-manager.ts L237-252](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/widget-manager.ts#L237-L252), [L290-302](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/widget-manager.ts#L290-L302); [widget-open-handler.ts L152-157](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/widget-open-handler.ts#L152-L157)); `serializeUri` strips fragments and normalizes `file:` so two line anchors into one file share one widget ([navigatable.ts L24-37](https://github.com/eclipse-theia/theia/blob/98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb/packages/core/src/browser/navigatable.ts#L24-L37)). VS Code's `extractSelection` lifts a `#L73,84` fragment into a selection applied after open ([opener.ts L147-160](https://github.com/microsoft/vscode/blob/a2f67035d5efaf16675905671a66593289e7f166/src/vs/platform/opener/common/opener.ts#L147-L160)).

What it replaces: `panelIdFor` in `ui/layout/zones.ts` builds `editor:<path>`, `editor:untitled-<n>`, `agent:<uuid>`, or a bare type by hand per type, and `layout-persistence.ts` stores those strings.

The fix: for artifact-backed panels, the panel id is a single function of the address (`run:53` from `runs/53`), fragments are excluded from the key and applied after open, and the rule that panel id and address are one transform apart holds for every kind rather than being a property of one.

Confidence: medium, two references, both strong human, but the subject's untitled and agent ids are not addresses today and need the workspace id counter to land first.

## Provenance

Three references carry explicit AI markers on cited files, and in every case the rewind shows the cited mechanism already present at the pre-AI form. VS Code's base markdown renderer (first AI commit 2025-11-05, a Copilot agent) kept `rewriteRenderedLinks`, `activateLink`, and the `data-href` guard intact and added a `stopPropagation` in the click path, a tightening; its label formatter interface and `findFormatting` selection are pre-AI and unchanged, while the home-template path (`resourceLabelTemplate.ts`, `findHomeFormatter`) is AI-originated in the three weeks before analysis and is excluded from Finding 5. Zed's `open_listener.rs`, `main.rs`, `zed.rs`, `client.rs`, and CLI files carry Claude, Copilot, Cursor, and Ona trailers from 2025-12 onward, all tree-wide refactors; the intake, parser, dispatcher, and `zed-cli://` rendezvous are identical at each pre-AI form, and the three later additions (`zed://skill`, the startup ordering guard, `CliResponseSink`) arrived by human-marked commits. Trilium's `link.ts`, its spec, the shared constants, the route table, and the image handler carry Claude trailers from 2026-06; the grammar, dispatcher, modifier table, title fallbacks, and id-keyed routes are present at each pre-AI form, the constants file is byte-identical, and the one AI-originated mechanism, the `splits` trust gate in `tab_manager.ts`, is not recommended here. Zotero and Theia have no AI-marked commit on any cited file. Element's cited files have no AI markers in reachable history but tag unknown because two 2026 tree moves truncate the rename-blind walk. The sample is six projects and about sixty files; it supports the per-idiom statements above and nothing broader.

## Where the subject already matches or beats the references

- The type-keyed slot registry with RAII `Registration` guards and boot-time `require` matches Theia's disposable-returning `addHandler` and VS Code's `registerOpener`; neither reference fails boot when a required contribution is missing, the subject does.
- The twin-pinned protocol with one shared JSON fixture beats Zed's three divergent URL-prefix allowlists and Element's four independent click-interception paths; the address fixture proposed in Finding 1 extends the subject's own idiom rather than importing one.
- The runtime exact-origin capability and tested navigation policy match Element's deep-link firewall and VS Code's opener validators as the trust boundary Finding 4 needs.
- The metadata-eager, code-lazy panel registry matches VS Code's provider activation join; a lazily loaded opener already has a home.

## Messes we should explicitly not copy

- VS Code: three uncoordinated startup URL queues with different expiry rules, one giving up after ten retries, one unbounded, one collected after thirty seconds; `CommandOpener` returns true both for handled and for refused-and-swallowed so the boolean carries two meanings; `resolveExternalUri` swallows resolver errors with a `noop` comment; multi-role files of 1,963, 1,658, and 1,323 lines.
- Zed: three divergent URL-prefix allowlists with no shared constant; an ordered prefix chain that lets `zed://agent` swallow `zed://agent/shared/{id}`, a URL the app itself emits; a Windows pipe that reads URLs into 128 bytes against 1,024 on Linux, so base64 payload links cannot survive it; no Windows registry key for the scheme while the register action is offered on every platform; a 3,054-line listener that absorbed the CLI session state machine.
- Zotero: a 1,219-line handler carrying eight hosts and a 365-line report assembler; cold-branch bugs including a registered `zotero://pdf.js` host whose constructor is called with its arguments in the wrong slots so it can never run; two URI namespaces that spell libraries differently, bridged by hand.
- Theia: an `addHandler` test that never awaits its promise, so the assertion cannot fail; a command handler returning -1 against a contract that says nonzero means can-open, saved only by an undocumented `> 0` filter; a dead parameter in `createUntitledURI`; a resource provider that swallows every resolver error into a generic not-registered.
- Trilium: a 777-line link module carrying grammar, DOM dispatch, title rendering, and load-time side effects; two disagreeing note-path parsers exported side by side; an unreachable `data` entry in the protocol allowlist; a duplicate download route; `TODO` plus `@ts-ignore` pairs on the core click handlers.
- Element: `encodeServerCandidates` copy-pasted into all three constructors and the `via` parse repeated five times; an abstract class faked with `throw new Error("Not implemented")`; a parser that works by constructing a fake constructor against `http://localhost`; four independent click-interception paths each writing `window.location.hash`; three schemes for one app; two `second-instance` listeners with unstated ordering.

## Recommended execution order

1. Finding 1: the address value type, its TypeScript mirror, and the shared fixture. Everything else parses or prints this type.
2. Finding 5: the noun table and `display_name`, since the fixture in step 1 should pin display names from the start and the panel title uses it immediately.
3. Finding 2 with Finding 7: the opener registry and the panel-id function, merged because both touch `zones.ts` and both replace the direct `openInZone` calls; land the registry, then re-key the artifact-backed panels.
4. Finding 3: the render-time `data-href` rewrite and the delegated click handler, once the opener exists to receive the address.
5. Finding 6: the artifact dispatcher and the provider slot, with the first provider listing only.
6. Finding 4: the intake, the firewall, and the ready-gated frame; the OS scheme registration waits for the bundle identifier and belongs to the shell plan.

## Refactor notes

Findings 1, 2, 5, and 7 are pure structure: they add a type, a registry, a table, and a keying function, and change no user-visible behavior until a caller is moved onto them. Findings 3, 4, and 6 change behavior: links stop navigating, a second launch stops discarding its arguments, and a new route family answers. The existing SPA and Rust test suites are the invariant; the shared fixture is added first and the existing `agent-frames.json` pattern is the model. Do not touch the shell's window capability, the cross-site guard, or the workspace path jail; the dispatcher mounts beside them and inherits them. The `panelIdFor` change in step 3 moves persisted layout keys; keep the old branches for `editor:` and `agent:` ids until their kinds have addresses. Verify and commit per step; two consecutive failures on one step stop the run for a re-plan.

## Sources

- https://github.com/microsoft/vscode - `a2f67035d5efaf16675905671a66593289e7f166` - MIT - analyzed 2026-09-16 - rewinds: `src/vs/base/browser/markdownRenderer.ts` pre-AI `04b38959df`, `src/vs/base/common/network.ts` pre-AI `ad3ca06ea4`, `src/vs/platform/label/common/label.ts` pre-AI `85e9d41a57`, `src/vs/workbench/services/label/common/labelService.ts` pre-AI `4dabc9bbfa`
- https://github.com/zed-industries/zed - `87f65de697bda93ad0a13dc92eabe941115b61de` - GPL-3.0-or-later - analyzed 2026-09-16 - rewinds: `crates/zed/src/zed/open_listener.rs` and `crates/zed/src/main.rs` pre-AI `3da926981c`, `crates/zed/src/zed.rs` pre-AI `975a76bbf0`, `crates/client/src/client.rs` pre-AI `075454fd07`, `crates/cli/src/main.rs` pre-AI `aa91fd4a96`, `crates/cli/src/cli.rs` pre-AI `e784c92e3c`, `crates/gpui/src/app.rs` pre-AI `8f961a41c6`, `crates/gpui_macos/src/platform.rs` pre-AI `de827bce2f`
- https://github.com/zotero/zotero - `c30163ed907bd17dd1ed66cc63eaaccad6471e5b` - AGPL-3.0 - analyzed 2026-09-16 - no rewinds
- https://github.com/eclipse-theia/theia - `98d1a268f03bc1a6709b0fbf6842c54c2ee13ccb` - EPL-2.0 OR GPL-2.0-only WITH Classpath-exception-2.0 - analyzed 2026-09-16 - no rewinds
- https://github.com/TriliumNext/Trilium - `1a2f27f8d3fb6bb80ed8de27692cca450d59a9d0` - AGPL-3.0-only - analyzed 2026-09-16 - rewinds: `apps/client/src/services/link.ts` pre-AI `937a13f5ea`, `link.spec.ts` pre-AI `e4a637ccbc`, `packages/commons/src/lib/shared_constants.ts` pre-AI `d5770e0d15`, `packages/trilium-core/src/routes/index.ts` pre-AI `7f52b47027`, `routes/api/image.ts` pre-AI `6531acf359`, `apps/client/src/components/tab_manager.ts` pre-AI `f4ecc0725b`, `app_context.ts` pre-AI `d7d411caf9`
- https://github.com/element-hq/element-web - `26771fd3b1576f8b35928ce8bba33a2ab6e7def4` (desktop code under `apps/desktop`) - AGPL-3.0-only OR GPL-3.0-only OR LicenseRef-Element-Commercial - analyzed 2026-09-16 - no rewinds
- Field survey and subject profile: 2026-09-16.

*2026-09-16 16:58 - Claude Fable 5.1 (Cursor agent)*
